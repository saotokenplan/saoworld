# 2026-07-23

## M4 规模化内容生成规划启动

- 用户要求更新 project-status.md 并直接开始 M4 规划。
- 现状盘点关键发现：M4 所需骨架已提前就位 —— generation-service 有 7 类生成器（npc/quest/region/settlement/monster/boss/item）+ 对应数据适配器 + quality_scorer + jinja2 模板；review-service 的 auto_review_engine 已实现 4 类规则（QualityScore/ContentSafety/FieldCompleteness/DuplicateDetection，对应原 S10-04）；`/ops/generation/regions` 与 `/ops/review/auto` API 均已存在。
- M4 真实差距：真实 LLM 质量稳定性、审核通过率 >70% 调优、发布自动化（审核→发布队列串联）、批量生成、周更运营流程。全部依赖真实运行时环境（与灰度发布共享验证前提）。
- 产出 1：更新 `docs/00-governance/project-status.md` —— 阶段改为"Sprint 9 收尾 + M4 规划启动"双线，新增 M4 项目级结论、阻塞表加"M4 排期需重基线（低）"、建议第 5 条改为规划已启动。
- 产出 2：新建 `docs/10-requirements/M4-规模化内容生成规划.md` —— 验收标准引用、现状盘点、8 项差距清单（G1-G8）、5 个工作包（WP1-WP5 + 验收包，保留 S10/S11 原始 ID 映射）、不抢跑原则（规划期只做低风险准备）、排期改依赖驱动（名义里程碑 2027-03-01 不变）。
- 注意：需求迭代计划中 Sprint 10/11 名义日期（2026.12）与实际进度（2026-07 收尾 Sprint 9）严重偏离，M4 实施排期待灰度窗口确认后在周报重基线。

## 自动化轮次：M4 规划期准备 WP2 双文档落地（01:55–02:20）

- 接续中断轮次：00:40 轮（auto-20260723-0040，审核样本集设计）内容完成后中断于提交前；本轮完成其提交/合并/推送/清理（merge 7594b27 + 11e0468 → origin/feature-prd）。
- 新任务 auto-20260723-0200：产出 `docs/10-requirements/M4-阈值调优预案.md`（draft）——引擎全参数空间清单、分场景调优预案 A-D、变更流程与不可调边界；merge cd201a7 + 535bcb0 → origin/feature-prd。
- 关键代码发现：`DuplicateDetectionRule.max_similarity=0.8` 是未接线死参数（evaluate 只用硬编码 0.3 多样度 + 20 字符门槛）；WP2「相似度 <0.8 实测」需先实施期改造接线，已登记。
- 工程经验：pre-commit 钩子校验提交信息格式（含合并提交），"Merge auto task: ..." 会被拒，须用 `<type>(<scope>): <summary>`，历史惯例 `docs(<scope>): merge auto-<id> <简述>`。
- M4 白名单进度：审核样本集设计 ✅、阈值调优预案 ✅；剩余：看板指标定义（WP3）、运营流程草案（WP4）、模板文本细化（WP1）。

## 自动化轮次：M4 看板指标定义 WP3 落地（03:10–03:25）

- 任务 auto-20260723-0310：产出 `docs/10-requirements/M4-审核效率看板指标定义.md`（draft）——K1 通过率 >70% / K2 审核耗时 P95 <30min / K3 人工介入率周对比三核心指标口径 + 双轨测量（离线验收轨锚定 WP2、在线运行轨 Prometheus）+ 看板四层布局 + 5 告警 / 2 SLO 草案。
- 六项代码/配置缺口登记（WP3 实施期改造项）：M1 无耗时埋点；M2 auto 路径 manual_review 不计入 reviews_total（routes.py L612 仅 is_automatic 计数）；M3 metrics.yaml 与代码标签不一致（reviews_total 声明多 risk_level、review_operations_total 声明 operation vs 代码 action）；M4 无 per-rule 指标；M5 dashboards/ 无实体 JSON；M6 alerts/SLO 无审核条目。
- 合并：e443f25（主）+ 88e63b4（回填）→ origin/feature-prd，均已校验；分支已清理。
- 工程经验：过程材料提交混入 .workbuddy 遥测会触发 commit-msg hook 的 scope 推断漂移（dev-loop→docs）导致拦截；遥测不进主题提交，留待周期性收拢（与 0040/0200 先例一致）。
- M4 白名单进度：样本集 ✅、阈值预案 ✅、看板指标 ✅；剩余：运营流程草案（WP4）、模板文本细化（WP1）。

## 04:43 轮次（auto-20260723-0443，M4 周更运营流程草案 WP4）
- 任务 auto-20260723-0443：产出 `docs/10-requirements/M4-周更运营流程草案.md`（draft）——周节奏 D1 组批 → D1–D3 自动审核 → D3–D4 抽检 → D5 灰度 → 次周一全量；批次放行门禁复用 WP3 口径；发布复用 OP-RELEASE-001/002/003 与既有状态机，不另造流程。
- 五项实施期差距登记：W1 审核→发布串联缺失（G6 核心）；W2 发布指标无埋点；W3 批次无建模；W4 运营人力未落实；W5 观察期口径未定。
- 合并：4d7c1f3（主）+ 16fb6ca（回填）→ origin/feature-prd，均已校验；分支已清理。
- M4 白名单进度：样本集 ✅、阈值预案 ✅、看板指标 ✅、运营流程 ✅；仅余模板文本细化（WP1），落地后规划期准备收口。

## 06:45 轮次（auto-20260723-0645，M4 模板文本细化 WP1——规划期收口）
- 任务 auto-20260723-0645：产出 `docs/10-requirements/M4-模板文本细化.md`（draft）——缺陷清单 F0–F6、装备/怪物数值锚定公式表、章节上限条款、输出格式硬化文本、双源收口方案、score_region 对齐表、实施期动作 A1–A6 与 WP1 验收路径。
- 关键实测发现（2 项高严重度）：
  - F0 双源提示词漂移：`services/generation/templates/` 16 个 jinja2 为死文本——`content_generator.py` 全部 `generate_*` 解析模板名后丢弃，实际走 `_build_*_prompt` 内联构造；Boss jinja2 缺评分器必填 6 字段（is_boss/boss_rank/phase_count/special_skills/enrage_threshold/reward，-0.18 分使 >0.85 不可达），live `_build_boss_prompt` 已含全部六字段。WP1 实测必须以 live prompt 为对象。
  - F1 `score_region` 校验 difficulty（easy/normal/hard/extreme）+ features，而 live prompt 与 region_data_adapter 用 danger_level（peaceful/…/extreme）+ landmarks——场景评分两维度名存实亡；实施期改评分器对齐（登记为 A2）。
- 合并：415c2f4（主）+ d954d5e（回填）→ origin/feature-prd，merge-base 校验通过；分支已清理。
- **里程碑：M4 规划期白名单五项全部落地，规划期准备收口**；M4 进入实施期待解锁状态（依赖灰度发布决策 + 运行时验证销项）。
- 后续常态：自动化循环预计回到「无新工作·优雅结束」，直至外部决策或新需求输入。

## 09:04 自动化轮次（auto-20260723-0904，无新工作·优雅结束）
- M4 规划期五项已收口，下一阶段建议 6 项均不可自主执行（#2 灰度决策硬阻塞；#3/#4 实测 docker/PG/Redis 均不可用；#5 实施侧不抢跑；#6 已自动化；#1 元治理）。
- 未创建分支/提交/合并/推送（节流，距上次真实提交 2.3h）；交付 auto-status-report-20260723-0904.md + 进度日志快照。

## 11:55 每日产品管理（PM Daily）

- 执行每日进展报告流程：读取全部关键文档 + 5 份当日执行摘要
- Sprint 9 完成率 20%（1/5：S9-05 公测文档已完成，S9-01~S9-04 依赖灰度发布决策）
- M4 规划期白名单五项已于当日全部落地，规划期准备收口
- 迭代方向评估：需关注（决策瓶颈）——技术方向正确，但灰度发布决策延迟是唯一硬阻塞
- 产出：`docs/40-dev-loop/daily-progress/daily-progress-2026-07-23.md`（日报）；更新 `docs/00-governance/project-status.md`（M4 规划期完成标记）
- Git：提交到 auto/pm-20260723，--no-ff 合并到 feature-prd，远程临时分支已删除
- 关键建议：立即召开灰度发布运营决策会议；停止在决策前进一步堆积规划文档

## 13:20 研发循环自动推进（auto-20260723-1320，无新工作·优雅结束）
- 判定：下一阶段建议 6 项均不可自主执行（#2 灰度决策唯一硬阻塞；#3/#4 实测 docker/PG/Redis 仍不可用；#5 规划期已收口且实施侧不抢跑；#6 已自动化；#1 元治理），与 12:13/09:04 连续持平。
- 距上次收拢合并（12:13）仅约 1.1h，按节流指引未创建分支/提交/合并/推送。
- 产出：`docs/40-dev-loop/auto-status-report-20260723-1320.md`；进度日志近期快照追加一条。
