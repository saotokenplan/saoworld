# 自动规划任务 - vote-service 审计日志持久化

> task_id: auto-20260702-0200
> 任务状态：已完成
> 规划时间：2026-07-02 02:00
> 完成时间：2026-07-02 02:25
> 触发来源：每小时自动推进

## 任务目标与背景

项目已完成 vote-service 核心投票链路、运营写接口和 JWT 鉴权中间件，但审计日志仅在结构化日志中记录，未持久化到 `audit_logs` 表。这是当前主要风险之一（`project-status.md` 明确标记）。

**本次目标**：为 vote-service 实现 `audit_logs` 表的模型、Repository 和路由集成，使关键业务操作（投票提交、投票周期创建与状态迁移）的审计记录持久化到数据库。

## 具体执行步骤

### 步骤 1：创建 AuditLog SQLAlchemy 模型

- 在 `app/domain/models.py` 中新增 `AuditLog` 模型
- 字段按 `backend-data-spec.md` 定义：audit_id, trace_id, request_id, operator_id, operator_role, action, resource_type, resource_id, reason, request_payload_jsonb, result_status, created_at
- CHECK 约束：operator_role IN ('player', 'ops', 'reviewer', 'system')
- 索引：trace_id, (operator_id, created_at), (resource_type, resource_id), (action, created_at)
- append-only 特征：无 updated_at 字段

### 步骤 2：创建 AuditRepository

- 新增 `app/repositories/audit_repo.py`
- 方法 `create_audit_log()`：写入审计记录
- 审计日志操作类型常量定义（ACTION_VOTE_SUBMIT, ACTION_VOTE_CYCLE_CREATE, ACTION_VOTE_CYCLE_TRANSITION）

### 步骤 3：为关键路由添加审计日志写入

- `submit_vote`：action=vote_submit, resource_type=vote, operator_role=player
- `create_vote_cycle`：action=vote_cycle_create, resource_type=vote_cycle, operator_role 从 JWT 提取
- 状态迁移接口（schedule/open/close/finalize）：action=vote_cycle_transition, resource_type=vote_cycle
- 修复了 SQLAlchemy identity map 导致 from_status 被覆盖的问题（在 transition 前捕获原始 status）

### 步骤 4：生成 Alembic 迁移脚本

- 为 audit_logs 表生成迁移脚本
- 包含所有字段、约束和索引

### 步骤 5：补充测试

- 测试投票提交时写入审计日志（含 trace_id 和无 trace_id 两种场景）
- 测试投票周期创建时写入审计日志
- 测试四种状态迁移（schedule/open/close/finalize）的审计日志
- 直接测试 AuditRepository
- 共 8 个新测试用例

### 步骤 6：更新项目状态

- 更新 `project-status.md`，标记审计日志持久化已完成
- 更新风险状态
- 更新测试数量和已落地资产清单

## 涉及文件与目录

- `services/vote/app/domain/models.py` - 新增 AuditLog 模型
- `services/vote/app/repositories/audit_repo.py` - 新增，审计日志 Repository
- `services/vote/app/api/routes.py` - 更新，添加审计日志写入
- `services/vote/alembic/versions/2026_07_02_0200_c8d2e5f1a730_add_audit_logs_table.py` - 新增迁移脚本
- `services/vote/tests/test_audit.py` - 新增，审计日志测试
- `services/vote/scripts/seed_dev_data.py` - 修复 ruff lint
- `docs/00-governance/project-status.md` - 状态更新

## 验收标准

- [x] AuditLog 模型字段与 `backend-data-spec.md` 一致
- [x] 投票提交时写入审计日志
- [x] 运营接口操作时写入审计日志
- [x] 审计日志包含正确的 operator_id、action、resource_type、trace_id 等字段
- [x] Alembic 迁移脚本包含 audit_logs 表
- [x] 所有测试通过（49/49，原 41 个 + 新增 8 个审计日志测试）
- [x] ruff check 通过
- [x] `project-status.md` 已同步更新
