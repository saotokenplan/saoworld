# 自动推进计划：代码质量修复 - ruff/mypy/测试问题集中修复

## 任务标识

- task_id：`auto-20260713-2200`
- 工作分支：`auto/auto-20260713-2200`
- 任务状态：执行中

## 任务目标与背景

项目 38 项建议已全部完成，Sprint 1/2/3/4 功能交付完整，P0/P1/P2/P3 阶段全部完成，项目处于灰度发布就绪状态。但全量代码质量扫描发现以下工程化问题需要修复：

1. **8 处 ruff 未使用导入/变量**（content、player、ops、gateway、workers）
2. **player-service 3 个 mypy 类型错误**（`create_audit_log` 的 `resource_id` 参数传入了 `str`，期望 `UUID | None`）
3. **ops-service 13 个 mypy 类型错误**（None 检查缺失、类型索引赋值问题）
4. **playtest 2 个集成测试失败**（`KeyError: 'data'`，响应结构不匹配）
5. **workers 弃用 API**（`datetime.utcnow()` 应改为 `datetime.now(UTC)`）

这些是灰度发布前的代码质量门禁问题，必须修复才能保证 CI 流水线全绿。

## 具体执行步骤

### 步骤 1：修复 ruff 未使用导入/变量

修复以下 8 处问题：
- `services/content/tests/test_content_packages.py:467` — 移除未使用的 `datetime` 和 `timezone` 导入
- `services/player/app/api/routes.py:103` — 移除未使用的 `MAX_PLAYER_LEVEL` 导入
- `services/ops/app/api/routes.py:14` — 移除未使用的 `calculate_quality_score` 和 `extract_insights_from_report` 导入
- `services/ops/app/core/insight_extractor.py:146` — 修复 `quest_events` 赋值后未使用
- `services/ops/app/core/requirement_generator.py:194` — 修复 `source_data` 赋值后未使用
- `services/gateway/app/schemas/events.py:2` — 移除未使用的 `UUID` 导入
- `workers/events/handlers.py:1` — 移除未使用的 `json` 导入
- `workers/tasks/player_event_ingestion.py` — 移除未使用的 `json`、`logging`、`AsyncSession` 导入

### 步骤 2：修复 player-service mypy 类型错误

修复 `services/player/app/api/routes.py` 中 `create_audit_log` 调用时 `resource_id` 参数类型不匹配：
- 将 `str` 类型的 resource_id 转换为 `UUID` 类型或使用 `None`

### 步骤 3：修复 ops-service mypy 类型错误

修复 `services/ops/app/api/routes.py` 中的类型问题：
- `dict[str, int] | None` 索引赋值前添加 None 检查
- `Insight | None` 属性访问前添加 None 检查

### 步骤 4：修复 playtest 集成测试失败

修复 `tools/playtest/test_vote_integration.py` 中 2 个失败测试：
- 分析响应结构不匹配的根因
- 调整测试断言以匹配实际服务响应格式

### 步骤 5：修复 workers 弃用 API

修复 `workers/events/event_bus.py` 中 `datetime.utcnow()` 改为 `datetime.now(UTC)`

### 步骤 6：全量验证

- 运行所有 8 个后端服务的 ruff 和 mypy 检查
- 运行所有测试确保无回归
- 更新项目状态文档

## 涉及的文件与目录

### 后端服务代码修复
- `services/content/tests/test_content_packages.py` — 移除未使用导入
- `services/player/app/api/routes.py` — 移除未使用导入 + 修复 mypy 类型
- `services/ops/app/api/routes.py` — 移除未使用导入 + 修复 mypy 类型
- `services/ops/app/core/insight_extractor.py` — 修复未使用变量
- `services/ops/app/core/requirement_generator.py` — 修复未使用变量
- `services/gateway/app/schemas/events.py` — 移除未使用导入

### Workers 修复
- `workers/events/handlers.py` — 移除未使用导入
- `workers/events/event_bus.py` — 修复弃用 API
- `workers/tasks/player_event_ingestion.py` — 移除未使用导入

### Playtest 修复
- `tools/playtest/test_vote_integration.py` — 修复集成测试

### 文档
- `docs/00-governance/project-status.md` — 更新当前阶段记录
- `docs/40-dev-loop/auto-plan-20260713-2200.md` — 本计划文档
- `docs/40-dev-loop/auto-execution-summary-20260713-2200.md` — 执行摘要
- `docs/40-dev-loop/auto-progress-log.md` — 追加进度记录

## 验收标准与 checklist

- [x] 所有 ruff 未使用导入/变量问题修复（0 个 ruff 错误）
- [x] player-service mypy 类型错误修复（0 个 mypy 错误）
- [x] ops-service mypy 类型错误修复（0 个 mypy 错误）
- [x] playtest 集成测试全部通过
- [x] workers 弃用 API 修复
- [x] 所有 8 个后端服务测试通过
- [x] project-status.md 更新完成
- [x] 执行摘要生成完成
- [x] 进度日志追加完成

## 任务状态

- 已完成

## 工作分支

- auto/auto-20260713-2200
