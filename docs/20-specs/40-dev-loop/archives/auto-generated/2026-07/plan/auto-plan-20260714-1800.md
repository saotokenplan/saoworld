# 任务计划：S7-01 第二章区域内容

> 任务标识：auto-20260714-1800
> 任务状态：已完成
> 工作分支：auto/auto-20260714-1800
> 创建时间：2026-07-14 18:00
> 完成时间：2026-07-14 18:30

## 任务目标与背景

### 目标

实现第二章区域内容扩展，新增2个扩展区域（幽光森林、南部绿洲），包含完整的区域配置、NPC和任务数据，为玩家提供更多探索空间和内容选择。

### 背景

Sprint 7 S7-02「装备系统」已完成，项目持续保持灰度发布就绪状态。S7-01「第二章区域内容」作为 P0 项，需要新增2个扩展区域并完成AI生成内容审核，为后续怪物生成（S7-03）和Boss战（S7-04）奠定内容基础。

### 需求来源

`docs/10-requirements/需求迭代计划.md` Sprint 7 S7-01「第二章区域内容」，验收标准：新增2个扩展区域，AI生成内容审核通过。

## 具体执行步骤

### 步骤 1：创建幽光森林区域数据文件

**目标**：创建幽光森林区域的完整配置数据。

**变更文件**：
- `game/data/regions/region_west_forest.json` - 新建文件

**实现内容**：
1. 区域基础信息（schema_version、region_id、name、description）
2. 区域属性（type、terrain、climate、resource_level、risk_level、status）
3. 势力分布（自由领地、铁卫联盟、暗影面纱）
4. 关键地点（4个：精灵古树、自由领地营地、月光湖、迷雾小径）
5. 可用任务列表
6. NPC列表

### 步骤 2：创建南部绿洲区域数据文件

**目标**：创建南部绿洲区域的完整配置数据。

**变更文件**：
- `game/data/regions/region_south_oasis.json` - 新建文件

**实现内容**：
1. 区域基础信息（schema_version、region_id、name、description）
2. 区域属性（type、terrain、climate、resource_level、risk_level、status）
3. 势力分布（丰收商会、自由领地）
4. 关键地点（4个：绿洲市场、商会总部、绿洲神庙、沙漠边缘）
5. 可用任务列表
6. NPC列表

### 步骤 3：为新区域创建NPC数据

**目标**：为幽光森林和南部绿洲创建NPC定义数据。

**变更文件**：
- `game/data/npcs/npc_list.json` - 扩展NPC数据

**实现内容**：
1. 幽光森林NPC（4个）：
   - npc_forest_leader（自由领地首领）
   - npc_forest_guide（向导）
   - npc_forest_healer（治疗师）
   - npc_forest_guard（守卫）
2. 南部绿洲NPC（4个）：
   - npc_oasis_merchant（商会会长）
   - npc_oasis_trader（商人）
   - npc_oasis_priest（神庙祭司）
   - npc_oasis_scout（侦察员）
3. 每个NPC包含完整对话树和任务关联

### 步骤 4：为新区域创建任务数据

**目标**：为幽光森林和南部绿洲创建任务链。

**变更文件**：
- `game/data/quests/quest_list.json` - 扩展任务数据

**实现内容**：
1. 幽光森林任务（3个）：
   - quest_forest_introduction（引导任务）
   - quest_forest_guardian（主线任务）
   - quest_forest_resource（支线任务）
2. 南部绿洲任务（3个）：
   - quest_oasis_trade（引导任务）
   - quest_oasis_secret（主线任务）
   - quest_oasis_caravan（支线任务）
3. 每个任务包含目标、奖励、对话触发条件

### 步骤 5：更新区域列表配置

**目标**：更新区域列表配置，激活新区域。

**变更文件**：
- `game/data/regions/region_list.json` - 更新区域状态

**实现内容**：
1. 将 region_west_forest 状态从 locked 改为 active
2. 将 region_south_oasis 状态从 locked 改为 active
3. 添加场景路径配置

### 步骤 6：验证数据完整性与兼容性

**目标**：验证新增区域数据的完整性和兼容性。

**变更文件**：
- `services/world/tests/test_world_regions.py` - 扩展区域测试

**实现内容**：
1. 测试新区域数据结构完整性
2. 测试区域与NPC关联
3. 测试区域与任务关联
4. 测试客户端区域加载
5. 测试区域解锁逻辑

## 验收标准

- [x] 幽光森林区域数据文件完整（region_west_forest.json）
- [x] 南部绿洲区域数据文件完整（region_south_oasis.json）
- [x] 新区域NPC数据完整（8个NPC：npc_forest_leader、npc_forest_guide、npc_forest_healer、npc_forest_guard、npc_oasis_merchant、npc_oasis_trader、npc_oasis_priest、npc_oasis_scout）
- [x] 新区域任务数据完整（5个任务：quest_forest_introduction、quest_forest_guardian、quest_oasis_caravan、quest_oasis_secret、quest_oasis_trade）
- [x] 区域列表配置更新正确（region_list.json 中两个区域状态改为 active）
- [x] 数据符合 schema_version 规范（所有区域数据 schema_version=1）

## 涉及的文件与目录

- `game/data/regions/region_west_forest.json`（新建）
- `game/data/regions/region_south_oasis.json`（新建）
- `game/data/npcs/npc_list.json`（修改）
- `game/data/quests/quest_list.json`（修改）
- `game/data/regions/region_list.json`（修改）
- `services/world/tests/test_world_regions.py`（修改）