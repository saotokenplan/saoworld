# 自动推进计划：Sprint 4 S4-01「投票结果落地展示」

## 任务标识

- task_id：`auto-20260713-1400`
- 工作分支：`auto/auto-20260713-1400`
- 任务状态：执行中

## 任务目标与背景

Sprint 3 玩家成长系统（S3-01 至 S3-05）已全部完成。Sprint 4 是下一个迭代，目标是完善投票体验，提升玩家对投票结果的感知和参与感。

S4-01「投票结果落地展示」是 Sprint 4 的首个 P0 任务，目标是在投票历史界面中展示投票结果的实际落地情况：

1. **落地标记显示**：在已落地投票周期旁显示"已落地"徽章
2. **内容跳转链接**：点击已落地投票可跳转到对应的内容包详情
3. **影响区域展示**：显示投票结果影响的区域和范围
4. **上线时间信息**：显示内容包的实际发布时间

本任务完成后，玩家能清楚看到自己投票的实际影响，增强参与感和成就感。

## 具体执行步骤

### 步骤 1：扩展 vote-service 投票历史 API

- 在 `services/vote/app/api/routes.py` 扩展 `GET /api/v1/votes/history` 接口：
  - 在响应中新增 `content_package` 字段（如已落地）
  - 新增 `landed_at` 字段（落地时间）
  - 新增 `affected_regions` 字段（影响区域列表）
- 扩展 `VoteHistoryResponse` schema 添加新字段
- 实现从 content-service 查询内容包信息的逻辑

### 步骤 2：实现 content-service 跨服务查询

- 在 vote-service 新增 `ContentPackageClient` 模块：
  - `get_content_package_by_vote_cycle()`：根据 vote_cycle_id 查询内容包
  - 支持缓存和超时配置
  - 错误处理（content-service 不可用时的降级）
- 添加 content-service 服务发现配置

### 步骤 3：扩展 Godot 客户端 VoteManager

- 在 `game/scripts/autoload/vote_manager.gd` 新增方法：
  - `get_vote_history_with_landing()`：获取带落地信息的投票历史
  - `is_vote_landed()`：判断投票是否已落地
  - `get_content_package_for_vote()`：获取投票对应的内容包信息
- 新增 `vote_landing_updated` 信号

### 步骤 4：优化 VoteHistoryPanel 界面显示

- 在 `game/scripts/ui/vote_history_panel.gd` 增强：
  - 新增 `LandedBadge` 节点显示"已落地"徽章
  - 新增 `ViewContentButton` 跳转到内容包详情
  - 新增 `AffectedRegionsLabel` 显示影响区域
  - 新增 `LandedAtLabel` 显示落地时间
- 实现点击跳转到内容包详情的逻辑
- 新增视觉样式：已落地投票使用金色边框

### 步骤 5：实现内容包详情弹窗

- 创建新场景 `game/scenes/ui/voting/ContentPackageDetail.tscn`
- 实现内容包详情弹窗脚本：
  - 显示内容包基本信息（版本号、发布时间）
  - 显示包含的区域、NPC、任务列表
  - 显示灰度范围和发布状态
- 从 ContentManager 获取内容包详情数据

### 步骤 6：补充测试用例

- vote-service 新增测试：
  - 投票历史 API 返回内容包信息的测试
  - ContentPackageClient 跨服务查询的测试
  - 降级逻辑测试（content-service 不可用）
- 客户端新增 GUT 测试：
  - VoteManager 落地信息方法测试
  - VoteHistoryPanel 落地徽章显示测试
  - 内容包详情弹窗测试

## 涉及的文件与目录

### 后端服务（vote-service）
- `services/vote/app/api/routes.py` - 扩展投票历史接口
- `services/vote/app/schemas/vote.py` - 扩展响应 schema
- `services/vote/app/core/content_client.py` - 新增内容包客户端
- `services/vote/app/core/config.py` - 添加 content-service 配置
- `services/vote/tests/test_vote_flow.py` - 新增测试用例

### Godot 客户端
- `game/scripts/autoload/vote_manager.gd` - 扩展落地信息方法
- `game/scripts/ui/vote_history_panel.gd` - 增强落地显示
- `game/scenes/ui/voting/ContentPackageDetail.tscn` - 新增内容包详情弹窗
- `game/scripts/ui/content_package_detail.gd` - 新增详情弹窗脚本
- `game/tests/test_vote_manager.gd` - 扩展测试用例

## 验收标准与 checklist

- [ ] vote-service 投票历史 API 返回内容包落地信息
- [ ] ContentPackageClient 正确查询 content-service
- [ ] 降级逻辑正确（content-service 不可用时不影响历史查询）
- [ ] VoteManager 正确获取和缓存落地信息
- [ ] VoteHistoryPanel 正确显示"已落地"徽章
- [ ] 点击已落地投票可跳转到内容包详情
- [ ] 内容包详情弹窗正确显示内容信息
- [ ] 所有新增测试用例通过
- [ ] ruff 和 mypy 检查通过
- [ ] 无回归问题（现有测试全部通过）

## 任务状态

- 部分完成（后端基础设施已实现，待后续迭代完成客户端集成）

## 工作分支

- auto/auto-20260713-1400