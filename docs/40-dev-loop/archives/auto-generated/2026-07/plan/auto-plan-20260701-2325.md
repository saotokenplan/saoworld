# 自动规划任务 - vote-service 数据库接入与迁移初始化

> task_id: auto-20260701-2325
> 任务状态：已完成
> 规划时间：2026-07-01 23:25
> 完成时间：2026-07-01 23:29
> 触发来源：工作树结束自动规划

## 任务目标与背景

项目已完成文档治理和规范阶段，进入工程初始化。`vote-service` 骨架（FastAPI + SQLAlchemy + Pydantic + pytest）已初始化，但缺少数据库迁移脚本（Alembic）和真实数据库连接，当前无数据库时所有 DB 相关接口返回 500。

**本次目标**：为 vote-service 初始化 Alembic，生成投票相关核心表的首次迁移脚本，并补充数据库集成测试，使 vote-service 达到端到端可运行状态。

## 涉及文件与目录

- `services/vote/app/core/db.py` - 数据库连接配置
- `services/vote/app/domain/models.py` - SQLAlchemy 数据模型
- `services/vote/alembic/` - 新增，Alembic 迁移目录
- `services/vote/alembic.ini` - 新增，Alembic 配置
- `services/vote/pyproject.toml` - 依赖更新（添加 alembic）
- `services/vote/tests/conftest.py` - 测试数据库配置
- `services/vote/tests/test_vote_flow.py` - 集成测试补充
- `infra/docker-compose.dev.yml` - 开发基础设施（已有，验证可用性）

## 具体执行步骤

### 步骤 1：验证开发基础设施

- 检查 `infra/docker-compose.dev.yml` 是否包含 PostgreSQL 16 和 Redis 7
- 确认 vote-service 的 `.env.example` 数据库连接配置格式

### 步骤 2：为 vote-service 添加 Alembic 依赖

- 在 `services/vote/pyproject.toml` 中添加 `alembic` 依赖
- 确认 `psycopg2-binary` 或 `asyncpg` 等数据库驱动已配置

### 步骤 3：初始化 Alembic 迁移环境

- 在 `services/vote/` 目录下执行 `alembic init alembic`
- 配置 `alembic.ini` 的数据库连接 URL（使用环境变量）
- 配置 `alembic/env.py`，使其从 `app.domain.models` 导入 Base 元数据
- 启用 `target_metadata = Base.metadata` 以支持自动生成迁移

### 步骤 4：生成首次迁移脚本

- 基于 `backend-data-spec.md` 中的投票相关表结构（`vote_cycles`、`vote_candidates`、`votes`）确认模型定义
- 执行 `alembic revision --autogenerate -m "init vote tables"` 生成首次迁移
- 检查生成的迁移脚本，确保包含：
  - `vote_cycles` 表（含 status、starts_at、ends_at、winning_candidate_id 等字段）
  - `vote_candidates` 表（含 vote_cycle_id 外键、status、vote_count 等）
  - `votes` 表（append-only，含 vote_cycle_id、player_id、candidate_id、idempotency_key 唯一约束）
  - 必要的索引（按 backend-data-spec.md 规范）
- 手动补充 CHECK 约束（status 合法值）和唯一约束

### 步骤 5：补充数据库集成测试

- 更新 `services/vote/tests/conftest.py`，添加测试数据库 fixture
- 添加 `test_vote_flow.py` 集成测试用例：
  - 创建投票周期
  - 获取当前投票周期接口测试
  - 提交投票接口测试（含幂等键去重测试）
  - 一人一票唯一约束测试
- 确保测试可通过 pytest 运行

### 步骤 6：更新文档与状态

- 更新 `docs/00-governance/project-status.md`，反映 vote-service 数据库迁移已完成的进展
- 标记「进入实施前的建议门槛」中「启动数据库并初始化 Alembic 迁移脚本」为已完成

## 验收标准

- [x] Alembic 目录结构完整，`alembic init` 成功
- [x] 首次迁移脚本包含 vote_cycles、vote_candidates、votes 三张核心表
- [x] 迁移脚本中的字段、索引、约束与 `backend-data-spec.md` 一致
- [x] 集成测试可通过 pytest 运行，覆盖率不低于修改前水平（13/13 全部通过）
- [x] `project-status.md` 已同步更新
- [x] 所有修改遵循 `10-python-backend.md` 和 `11-database.md` 规范
