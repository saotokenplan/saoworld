# 自动规划任务 - 多服务 Alembic 数据库迁移环境初始化

> task_id: auto-20260704-0200
> 任务状态：已完成
> 规划时间：2026-07-04 02:00
> 触发来源：每小时自动推进
> 工作分支：auto/auto-20260704-0200

## 任务目标与背景

vote-service、world-service、content-service、generation-service、review-service、gateway-service、player-service 和 ops-service 八个核心服务已完整实现并通过验证。但目前只有 vote-service 初始化了 Alembic 数据库迁移环境，其他 6 个服务（world、content、generation、review、player、ops）均缺少迁移脚本。

根据 `11-database.md` 规范，数据库迁移是基础设施的重要组成部分，用于管理表结构变更、保证多环境一致性、支持版本化回滚。vote-service 已验证 Alembic + SQLAlchemy 异步模式可行，可作为模板推广到其他服务。

**本次目标**：为 world-service、content-service、generation-service、review-service、player-service、ops-service 六个服务初始化 Alembic 数据库迁移环境，生成首次迁移脚本（核心业务表 + 审计日志表），对齐 vote-service 的迁移模式，为后续数据库变更管理奠定基础。

## 具体执行步骤

### 步骤 1：world-service Alembic 初始化

- 复制 `services/vote/alembic/` 目录结构到 `services/world/`
- 创建 `services/world/alembic.ini` 配置文件
- 调整 `env.py` 导入路径（使用 world-service 的 `app.core.db`、`app.core.config`、`app.domain.models`）
- 生成首次迁移脚本：`init_world_tables`（regions 表）
- 生成第二次迁移脚本：`add_audit_logs_table`（audit_logs 表）

### 步骤 2：content-service Alembic 初始化

- 复制 `services/vote/alembic/` 目录结构到 `services/content/`
- 创建 `services/content/alembic.ini` 配置文件
- 调整 `env.py` 导入路径
- 生成首次迁移脚本：`init_content_tables`（regions、vote_cycles、content_packages、release_records、rollback_records 表）
- 生成第二次迁移脚本：`add_audit_logs_table`（audit_logs 表）

### 步骤 3：generation-service Alembic 初始化

- 复制 `services/vote/alembic/` 目录结构到 `services/generation/`
- 创建 `services/generation/alembic.ini` 配置文件
- 调整 `env.py` 导入路径
- 生成首次迁移脚本：`init_generation_tables`（generation_requests、generated_objects 表）
- 生成第二次迁移脚本：`add_audit_logs_table`（audit_logs 表）

### 步骤 4：review-service Alembic 初始化

- 复制 `services/vote/alembic/` 目录结构到 `services/review/`
- 创建 `services/review/alembic.ini` 配置文件
- 调整 `env.py` 导入路径
- 生成首次迁移脚本：`init_review_tables`（review_records 表）
- 生成第二次迁移脚本：`add_audit_logs_table`（audit_logs 表）

### 步骤 5：player-service Alembic 初始化

- 复制 `services/vote/alembic/` 目录结构到 `services/player/`
- 创建 `services/player/alembic.ini` 配置文件
- 调整 `env.py` 导入路径（注意 player-service 使用自定义 UUIDType）
- 生成首次迁移脚本：`init_player_tables`（players、player_quests、player_regions 表）
- 生成第二次迁移脚本：`add_audit_logs_table`（audit_logs 表）

### 步骤 6：ops-service Alembic 初始化

- 复制 `services/vote/alembic/` 目录结构到 `services/ops/`
- 创建 `services/ops/alembic.ini` 配置文件
- 调整 `env.py` 导入路径
- 生成首次迁移脚本：`init_ops_tables`（ops_dashboards、ops_actions 表）
- 生成第二次迁移脚本：`add_audit_logs_table`（audit_logs 表）

### 步骤 7：更新各服务 pyproject.toml 依赖

- 为 6 个服务添加 `alembic` 可选依赖
- 确保 SQLAlchemy 异步驱动依赖正确
- 验证每个服务的 pytest 测试仍然通过

### 步骤 8：质量验证

- 检查每个服务的迁移脚本语法正确
- 验证迁移脚本中 CHECK 约束与模型一致
- 验证索引、外键、默认值与模型定义一致
- 运行各服务现有测试确保不破坏现有功能
- 运行 ruff check 代码检查

## 涉及文件与目录

- `services/world/`
  - `alembic.ini`
  - `alembic/`
    - `env.py`
    - `script.py.mako`
    - `README`
    - `versions/`
      - `*_init_world_tables.py`
      - `*_add_audit_logs_table.py`
- `services/content/`
  - `alembic.ini`
  - `alembic/`
    - `env.py`
    - `script.py.mako`
    - `README`
    - `versions/`
      - `*_init_content_tables.py`
      - `*_add_audit_logs_table.py`
- `services/generation/`
  - `alembic.ini`
  - `alembic/`
    - `env.py`
    - `script.py.mako`
    - `README`
    - `versions/`
      - `*_init_generation_tables.py`
      - `*_add_audit_logs_table.py`
- `services/review/`
  - `alembic.ini`
  - `alembic/`
    - `env.py`
    - `script.py.mako`
    - `README`
    - `versions/`
      - `*_init_review_tables.py`
      - `*_add_audit_logs_table.py`
- `services/player/`
  - `alembic.ini`
  - `alembic/`
    - `env.py`
    - `script.py.mako`
    - `README`
    - `versions/`
      - `*_init_player_tables.py`
      - `*_add_audit_logs_table.py`
- `services/ops/`
  - `alembic.ini`
  - `alembic/`
    - `env.py`
    - `script.py.mako`
    - `README`
    - `versions/`
      - `*_init_ops_tables.py`
      - `*_add_audit_logs_table.py`

## 验收标准

- [x] world-service Alembic 环境初始化完成，迁移脚本生成
- [x] content-service Alembic 环境初始化完成，迁移脚本生成
- [x] generation-service Alembic 环境初始化完成，迁移脚本生成
- [x] review-service Alembic 环境初始化完成，迁移脚本生成
- [x] player-service Alembic 环境初始化完成，迁移脚本生成
- [x] ops-service Alembic 环境初始化完成，迁移脚本生成
- [x] 每个迁移脚本包含正确的表结构、CHECK 约束、索引、外键
- [x] 每个服务的 env.py 正确导入模型和配置
- [x] alembic.ini 配置正确（script_location、file_template 等）
- [x] 各服务现有测试全部通过
- [x] ruff check 通过
- [x] project-status.md 已同步更新
