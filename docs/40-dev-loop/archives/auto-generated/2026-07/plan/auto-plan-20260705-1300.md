# 自动规划任务 - 迁移各服务 routes.py 使用统一错误码模块

> task_id: auto-20260705-1300
> 任务状态：已完成
> 规划时间：2026-07-05 13:00
> 完成时间：2026-07-05 13:00
> 触发来源：每小时自动推进
> 工作分支：auto/auto-20260705-1300

## 任务目标与背景

**当前阶段**：内容发布与验证阶段（端到端集成验证完成）

**当前目标**：完成首期内容包灰度发布，验证端到端玩法流程，进入内容生成与投票驱动世界更新的闭环

**发现的问题**：

1. **P1 - 各服务 routes.py 尚未迁移到使用统一错误码模块**：
   - 上一轮任务（auto-20260705-1200）已为 8 个后端服务创建了 `app/core/errors.py` 错误码模块
   - 但各服务的 routes.py 仍然直接使用硬编码字符串错误码
   - 需要将 routes.py 中的错误处理迁移到使用统一的错误码常量和 `raise_*_error()` 函数

2. **P1 - 测试验证不完整**：
   - 上一轮只验证了 vote-service 的 54 个测试
   - 其他 7 个服务的测试尚未运行验证
   - 需要运行所有服务的测试确保迁移后功能正常

3. **P2 - 客户端错误码映射未同步**：
   - Godot 客户端 `APIManager.gd` 中的错误码处理需要与服务端对齐

**本次目标**：
1. 迁移 8 个后端服务的 routes.py 使用统一错误码模块
2. 确保所有服务测试通过
3. 同步更新客户端错误码映射
4. 更新项目状态文档

## 具体执行步骤

### 步骤 1：迁移 vote-service routes.py

- 读取 `services/vote/app/api/routes.py`
- 将硬编码错误码替换为 `VoteErrorCodes` 常量
- 使用 `raise_vote_error()` 函数统一抛出异常
- 运行 vote-service 测试验证

### 步骤 2：迁移 world-service routes.py

- 读取 `services/world/app/api/routes.py`
- 将硬编码错误码替换为 `WorldErrorCodes` 常量
- 使用 `raise_world_error()` 函数统一抛出异常
- 运行 world-service 测试验证

### 步骤 3：迁移 content-service routes.py

- 读取 `services/content/app/api/routes.py`
- 将硬编码错误码替换为 `ContentErrorCodes` 常量
- 使用 `raise_content_error()` 函数统一抛出异常
- 运行 content-service 测试验证

### 步骤 4：迁移 generation-service routes.py

- 读取 `services/generation/app/api/routes.py`
- 将硬编码错误码替换为 `GenerationErrorCodes` 常量
- 使用 `raise_generation_error()` 函数统一抛出异常
- 运行 generation-service 测试验证

### 步骤 5：迁移 review-service routes.py

- 读取 `services/review/app/api/routes.py`
- 将硬编码错误码替换为 `ReviewErrorCodes` 常量
- 使用 `raise_review_error()` 函数统一抛出异常
- 运行 review-service 测试验证

### 步骤 6：迁移 player-service routes.py

- 读取 `services/player/app/api/routes.py`
- 将硬编码错误码替换为 `PlayerErrorCodes` 常量
- 使用 `raise_player_error()` 函数统一抛出异常
- 运行 player-service 测试验证

### 步骤 7：迁移 ops-service routes.py

- 读取 `services/ops/app/api/routes.py`
- 将硬编码错误码替换为 `OpsErrorCodes` 常量
- 使用 `raise_ops_error()` 函数统一抛出异常
- 运行 ops-service 测试验证

### 步骤 8：迁移 gateway-service routes.py

- 读取 `services/gateway/app/api/routes.py` 和 `services/gateway/app/core/proxy.py`
- 将硬编码错误码替换为 `GatewayErrorCodes` 常量
- 使用 `raise_gateway_error()` 函数统一抛出异常
- 运行 gateway-service 测试验证

### 步骤 9：更新客户端错误码映射

- 读取 `game/scripts/autoload/APIManager.gd`
- 更新错误码映射与服务端对齐
- 确保客户端能正确识别和处理各类错误

### 步骤 10：运行所有测试

- 运行所有 8 个后端服务的测试
- 运行 workers 测试
- 运行内容检查工具测试
- 确保所有测试通过

### 步骤 11：更新项目状态文档

- 更新 `docs/00-governance/project-status.md`
- 更新错误码统一工作的完成状态
- 更新进度日志

## 涉及文件与目录

### 修改文件
- `services/vote/app/api/routes.py`
- `services/world/app/api/routes.py`
- `services/content/app/api/routes.py`
- `services/generation/app/api/routes.py`
- `services/review/app/api/routes.py`
- `services/player/app/api/routes.py`
- `services/ops/app/api/routes.py`
- `services/gateway/app/api/routes.py`
- `services/gateway/app/core/proxy.py`
- `game/scripts/autoload/APIManager.gd`
- `docs/00-governance/project-status.md`

## 验收标准

- [x] vote-service routes.py 迁移完成，测试通过
- [x] world-service routes.py 迁移完成，测试通过
- [x] content-service routes.py 迁移完成，测试通过
- [x] generation-service routes.py 迁移完成，测试通过
- [x] review-service routes.py 迁移完成，测试通过
- [x] player-service routes.py 迁移完成，测试通过
- [x] ops-service routes.py 迁移完成，测试通过
- [x] gateway-service routes.py 迁移完成，测试通过
- [x] 客户端错误码映射已更新
- [x] 所有服务测试全部通过
- [x] 项目状态文档已更新

## 风险与注意事项

- 修改错误码属于破坏性变更，需确保测试覆盖充分
- 客户端需要同步更新错误码映射
- 修改范围涉及 8 个服务，需逐一验证
- 确保错误响应格式完全符合 12-api-design.md 规范
- 测试中硬编码的错误码需要同步更新