# 自动任务计划 - auto-20260705-2000

> 任务状态：已完成
> 工作分支：auto/auto-20260705-2000
> 创建时间：2026-07-05 20:00

## 任务标识

- **task_id**: auto-20260705-2000

## 任务目标与背景

### 目标

修复所有后端服务和 workers 的 mypy 类型错误，让 CI 中的 mypy 类型检查门禁真正生效（移除 `|| true` 绕过），提升代码类型安全性。

### 背景

当前项目已进入内容发布与验证阶段，所有核心功能均已实现，349 个测试用例全部通过。但 CI 配置中的 mypy 类型检查使用了 `|| true` 绕过，原因是各服务存在较多类型错误：

| 服务 | 错误数量 |
|------|---------|
| vote | 27 |
| world | 13 |
| content | 25 |
| generation | 38 |
| review | 20 |
| gateway | 4 |
| player | 1 |
| ops | 0 |
| workers | 1 |

主要错误类型：
1. `union-attr`: Optional 类型未做 None 检查直接访问属性
2. `import-not-found`: 缺少 redis 等依赖的类型桩
3. 其他类型不匹配问题

这些类型错误导致 mypy 门禁无法真正生效，降低了代码质量保障。

## 具体执行步骤

### 步骤 1：修复 vote-service mypy 错误

- 修复 `app/api/routes.py` 中的 union-attr 错误（Optional 类型 None 检查）
- 修复 `app/core/event_publisher.py` 中的 redis import-not-found（添加类型忽略或安装类型桩）
- 验证：`cd services/vote && mypy app` 0 错误

### 步骤 2：修复 world-service mypy 错误

- 检查并修复 world-service 所有 mypy 错误
- 验证：`cd services/world && mypy app` 0 错误

### 步骤 3：修复 content-service mypy 错误

- 检查并修复 content-service 所有 mypy 错误
- 验证：`cd services/content && mypy app` 0 错误

### 步骤 4：修复 generation-service mypy 错误

- 检查并修复 generation-service 所有 mypy 错误
- 验证：`cd services/generation && mypy app` 0 错误

### 步骤 5：修复 review-service mypy 错误

- 检查并修复 review-service 所有 mypy 错误
- 验证：`cd services/review && mypy app` 0 错误

### 步骤 6：修复 gateway-service mypy 错误

- 检查并修复 gateway-service 所有 mypy 错误
- 验证：`cd services/gateway && mypy app` 0 错误

### 步骤 7：修复 player-service mypy 错误

- 检查并修复 player-service 所有 mypy 错误
- 验证：`cd services/player && mypy app` 0 错误

### 步骤 8：修复 workers mypy 错误

- 检查并修复 workers 所有 mypy 错误
- 验证：`cd workers && mypy .` 0 错误

### 步骤 9：更新 CI 配置

- 移除 `.github/workflows/ci.yml` 中 mypy 的 `|| true` 绕过
- 让 mypy 成为真正的阻塞门禁

### 步骤 10：运行所有测试验证

- 确保所有服务的 pytest 测试仍然通过
- 确保 ruff lint 仍然通过

## 涉及的文件与目录

### 修改文件
- `services/vote/app/api/routes.py`
- `services/vote/app/core/event_publisher.py`
- `services/vote/pyproject.toml`（如需配置 mypy ignore）
- `services/world/app/api/routes.py`
- `services/world/app/core/event_publisher.py`
- `services/content/app/api/routes.py`
- `services/content/app/core/event_publisher.py`
- `services/generation/app/api/routes.py`
- `services/generation/app/core/event_publisher.py`
- `services/review/app/api/routes.py`
- `services/review/app/core/event_publisher.py`
- `services/gateway/app/` 相关文件
- `services/player/app/` 相关文件
- `workers/` 相关文件
- `.github/workflows/ci.yml`
- `docs/00-governance/project-status.md`

## 验收标准与 Checklist

### 验收标准

1. 所有 8 个后端服务 + workers 的 mypy 检查 0 错误
2. CI 配置移除 mypy 的 `|| true` 绕过
3. 所有服务的 pytest 测试仍然通过
4. ruff lint 检查通过
5. 项目状态文档更新记录

### Checklist

- [x] 修复 vote-service mypy 错误（27个）
- [x] 修复 world-service mypy 错误（13个）
- [x] 修复 content-service mypy 错误（25个）
- [x] 修复 generation-service mypy 错误（38个）
- [x] 修复 review-service mypy 错误（20个）
- [x] 修复 gateway-service mypy 错误（4个）
- [x] 修复 player-service mypy 错误（1个）
- [x] 修复 workers mypy 错误（实际33个，已全部修复）
- [x] 更新 CI 配置，移除 mypy 的 `|| true`，修正 workers mypy 路径
- [x] 运行所有服务 pytest 验证通过（vote:54, world:43, content:58, generation:50, review:41, gateway:35, player:26, ops:35）
- [x] 修复 datetime.utcnow() 弃用警告（vote/content/generation/review 服务）
- [x] 更新 project-status.md
- [x] 生成执行摘要
- [x] 更新进度日志
