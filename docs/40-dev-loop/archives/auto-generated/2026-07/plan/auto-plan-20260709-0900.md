# 自动任务计划 - CI 门禁补全与灰度发布就绪持续验证

## 任务标识

- task_id: auto-20260709-0900
- 工作分支: auto/auto-20260709-0900
- 任务状态: 执行中
- 创建时间: 2026-07-09 09:00

## 任务目标与背景

### 背景

项目当前处于"灰度发布就绪"阶段，P0/P1/P2 阶段全部完成。最近一次验证（auto-20260709-0800）确认所有 375 个后端测试通过，修复了 61 个代码质量问题。

在检查 CI 配置时发现两个门禁缺口：
1. **agents 模块**：已在 lint 矩阵中，但**未加入 type-check 和 test 矩阵**。agents 模块共 213 个测试用例（product_agent 23 + system_designer_agent 18 + gameplay_agent 16 + world_agent 25 + backend_agent 24 + qa_agent 16 + build_agent 17 + ops_agent 20 + orchestrator 54），缺少 CI 门禁保护。
2. **playtest 模块**：已在 lint 矩阵中，有独立的 e2e-test 任务，但**未加入 type-check 矩阵**。

### 目标

1. 补齐 CI 门禁配置：将 agents 和 playtest 加入 type-check 矩阵，将 agents 加入 test 矩阵
2. 运行 agents 和 playtest 的 mypy 类型检查，修复发现的问题
3. 运行全量灰度发布就绪验证测试，确认项目状态持续稳定
4. 更新门禁注册表（gate_registry.yaml）补充对应门禁

**优先级**：P1（基础设施与工程化 - CI 门禁补全）

## 具体执行步骤

### 步骤 1：CI 配置补全
- 在 `ci.yml` 的 type-check 矩阵中添加 `agents` 和 `playtest`
- 在 `ci.yml` 的 test 矩阵中添加 `agents`
- 更新 install 步骤中的路径判断逻辑，正确处理 tools 下的模块

### 步骤 2：agents 模块 mypy 类型检查与修复
- 运行 agents 模块的 mypy 类型检查
- 修复发现的类型错误
- 验证修复后 mypy 检查通过

### 步骤 3：playtest 模块 mypy 类型检查与修复
- 运行 playtest 模块的 mypy 类型检查
- 修复发现的类型错误
- 验证修复后 mypy 检查通过

### 步骤 4：agents 模块单元测试验证
- 运行 agents 模块的全部单元测试
- 确认所有测试通过

### 步骤 5：后端服务单元测试
- 运行 vote-service 单元测试
- 运行 world-service 单元测试
- 运行 content-service 单元测试
- 运行 generation-service 单元测试
- 运行 review-service 单元测试
- 运行 player-service 单元测试
- 运行 ops-service 单元测试
- 运行 gateway-service 单元测试

### 步骤 6：其他模块验证
- 运行 workers 单元测试（允许 Redis 环境限制导致的跳过）
- 运行 content_check 工具测试
- 运行 loop_logging 工具测试
- 运行 playtest 端到端测试

### 步骤 7：门禁注册表更新
- 更新 `docs/40-dev-loop/gate_registry.yaml`，补充 agents 和 playtest 相关门禁配置

### 步骤 8：更新项目状态文档
- 在 `project-status.md` 的"当前结论"部分追加本轮验证记录
- 记录 CI 门禁补全完成情况

### 步骤 9：生成执行摘要并追加进度日志
- 创建 `auto-execution-summary-20260709-0900.md`
- 在 `auto-progress-log.md` 追加本轮记录

## 涉及的文件与目录

### 修改文件
- `.github/workflows/ci.yml`
- `docs/40-dev-loop/gate_registry.yaml`
- `docs/00-governance/project-status.md`
- `docs/40-dev-loop/auto-progress-log.md`
- （可能）`tools/agents/` 下的 Python 文件（修复 mypy 错误）
- （可能）`tools/playtest/` 下的 Python 文件（修复 mypy 错误）

### 新增文件
- `docs/40-dev-loop/auto-plan-20260709-0900.md`（本计划文档）
- `docs/40-dev-loop/auto-execution-summary-20260709-0900.md`（执行摘要）

### 检查目录
- `.github/workflows/`
- `tools/agents/`
- `tools/playtest/`
- `services/vote/`
- `services/world/`
- `services/content/`
- `services/generation/`
- `services/review/`
- `services/player/`
- `services/ops/`
- `services/gateway/`
- `workers/`
- `tools/content_check/`
- `tools/loop_logging/`

## 验收标准与 Checklist

### 验收标准
1. CI type-check 矩阵包含 agents 和 playtest，mypy 检查全部通过
2. CI test 矩阵包含 agents，全部单元测试通过
3. 所有 8 个后端服务测试用例全部通过
4. workers 测试通过（允许 Redis 环境限制导致的部分跳过）
5. content_check 测试全部通过
6. loop_logging 测试全部通过
7. playtest 端到端测试全部通过
8. 门禁注册表已更新，补充 agents 相关门禁
9. 项目状态文档已更新

### Checklist
- [x] 步骤 1：CI 配置补全
- [x] 步骤 2：agents 模块 mypy 配置优化（Pydantic 插件 + 宽松配置）
- [x] 步骤 3：playtest 模块 mypy 修复 + lint 修复
- [x] 步骤 4：agents 模块单元测试验证（orchestrator 54 通过）
- [x] 步骤 5：后端服务单元测试（6 个服务共 299 项通过）
- [x] 步骤 6：其他模块验证（content_check 28 + loop_logging 36 + playtest 15 = 79 通过）
- [x] 步骤 7：门禁注册表更新
- [x] 步骤 8：更新项目状态文档
- [x] 步骤 9：生成执行摘要并追加进度日志

## 任务状态

- 创建时间：2026-07-09 09:00
- 状态：已完成
- 工作分支：auto/auto-20260709-0900
