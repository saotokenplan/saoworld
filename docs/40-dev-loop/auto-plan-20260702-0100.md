# 自动规划任务 - vote-service 端到端验证与运营接口补充

> task_id: auto-20260702-0100
> 任务状态：已完成
> 完成时间：2026-07-02 01:00
> 规划时间：2026-07-02 01:00
> 触发来源：自动推进循环

## 任务目标与背景

项目已完成 vote-service 骨架初始化和 Alembic 迁移脚本生成（auto-20260701-2325）。根据 `project-status.md` 下一阶段建议第 4 项："启动本地 PostgreSQL 并执行迁移，完成 vote-service 端到端可运行验证"。

当前代码存在以下阻碍端到端验证的问题：
1. **API 响应未遵循统一 envelope 格式**：`12-api-design.md` 要求所有响应使用 `{request_id, data, meta, trace_id}` 包装，但当前各接口直接返回裸业务数据
2. **weight 校验与 CHECK 约束不一致**：schema 中 `ge=0.0` 允许 weight=0，但数据库 CHECK 约束要求 `weight > 0`
3. **缺少运营接口**：无法通过 API 创建投票周期，端到端流程不完整

**本次目标**：修复上述问题，补充运营写接口（创建投票周期），使 vote-service 达到端到端可运行验证状态。

## 涉及文件与目录

- `services/vote/app/api/routes.py` - API 路由（envelope 格式修复 + 运营接口）
- `services/vote/app/schemas/vote.py` - Pydantic 模型（envelope schema + weight 修复 + 运营 schema）
- `services/vote/app/repositories/vote_repo.py` - 数据访问层（运营相关方法）
- `services/vote/app/main.py` - 异常处理（envelope 适配）
- `services/vote/tests/test_vote_flow.py` - 集成测试更新
- `services/vote/tests/test_health.py` - 集成测试更新
- `services/vote/tests/test_ops.py` - 新增，运营接口测试

## 具体执行步骤

### 步骤 1：修复 Pydantic schema - weight 校验与 envelope 模型

- 修复 `VoteSubmitRequest.weight` 从 `ge=0.0` 改为 `gt=0`
- 新增统一的响应 envelope 模型：`EnvelopeResponse`、`PaginatedEnvelopeResponse`
- 调整所有响应模型为嵌套在 `data` 字段内的格式

### 步骤 2：修复 API 路由 - envelope 响应格式

- 所有成功响应统一使用 envelope 包装，包含 `request_id`、`data`、`trace_id`
- 列表接口包含 `meta` 分页信息
- 错误响应保持现有格式（已符合规范）

### 步骤 3：添加运营接口 Repository 方法

- `create_vote_cycle()` - 创建投票周期
- `add_candidate()` - 添加候选项

### 步骤 4：添加运营接口 schemas

- `CreateVoteCycleRequest` - 创建投票周期请求
- `AddCandidateRequest` - 添加候选项请求

### 步骤 5：添加运营接口路由

- `POST /api/v1/ops/vote-cycles` - 创建投票周期（含候选项）
- 需要 `Idempotency-Key` 和 `X-Trace-Id` 请求头

### 步骤 6：更新测试

- 更新现有测试以适配 envelope 响应格式
- 新增运营接口测试
- 新增 envelope 格式验证测试

### 步骤 7：运行全部测试确保通过

## 验收标准

- [x] 所有成功响应使用统一 envelope 格式（request_id/data/meta/trace_id）
- [x] weight 校验与 CHECK 约束一致（gt=0）
- [x] 运营接口 POST /api/v1/ops/vote-cycles 可正常创建投票周期
- [x] 所有现有测试适配新格式并继续通过
- [x] 新增运营接口测试覆盖
- [x] 代码遵循 `10-python-backend.md`、`11-database.md`、`12-api-design.md` 规范
