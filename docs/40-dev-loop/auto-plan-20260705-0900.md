# 自动规划任务 - 服务间事件发布集成（打通端到端事件驱动链路）

> task_id: auto-20260705-0900
> 任务状态：已完成
> 规划时间：2026-07-05 09:00
> 触发来源：每小时自动推进
> 工作分支：auto/auto-20260705-0900

## 任务目标与背景

**当前阶段**：内容发布与验证阶段（首期内容包已打包）
**当前目标**：完成首期内容包灰度发布，验证端到端玩法流程

**发现的问题**：

1. **P0 - 事件发布未集成到业务代码**：
   - `auto-20260705-0800` 已创建事件总线基础设施（`workers/events/`）
   - 已创建各服务的事件发布客户端（`event_publisher.py`）
   - 但各服务的 `routes.py` 尚未调用事件发布方法
   - 投票结算、内容发布/回滚、生成完成、审核完成等关键操作点未触发事件
   - 这导致事件驱动链路（投票→生成→审核→打包→发布）无法真正运转

2. **P0 - 事件消费重试机制缺失**：
   - `workers/events/event_subscriber.py` 的事件处理失败后没有重试逻辑
   - 单次消费失败会导致整个链路中断
   - 需要实现重试机制（带指数退避）和死信队列处理

**本次目标**：
1. 在 vote-service 的关键操作点集成事件发布（投票周期关闭、投票结果结算）
2. 在 content-service 的关键操作点集成事件发布（内容包发布、内容包回滚）
3. 在 generation-service 的关键操作点集成事件发布（生成请求创建、批量生成完成）
4. 在 review-service 的关键操作点集成事件发布（批量审核完成）
5. 实现事件消费重试机制（指数退避 + 死信队列）
6. 更新项目状态文档
7. 补充测试用例

## 具体执行步骤

### 步骤 1：查看各服务的 event_publisher.py 和 routes.py

- 读取 vote-service、content-service、generation-service、review-service 的 event_publisher.py
- 读取各服务的 routes.py，确定需要集成事件发布的关键操作点

### 步骤 2：集成 vote-service 事件发布

- 在投票周期关闭时发布 `vote.cycle.closed` 事件
- 在投票结果结算完成时发布 `vote.result.finalized` 事件
- 确保事件 payload 包含必要字段（cycle_id、winning_candidate_id、trace_id 等）

### 步骤 3：集成 content-service 事件发布

- 在内容包发布时发布 `content.package.released` 事件
- 在内容包回滚时发布 `content.package.rolled_back` 事件
- 确保事件 payload 包含必要字段（package_id、release_mode、gray_scope、trace_id 等）

### 步骤 4：集成 generation-service 事件发布

- 在生成请求创建时发布 `generation.request.created` 事件
- 在批量生成完成时发布 `generation.batch.completed` 事件
- 确保事件 payload 包含必要字段（request_id、generated_objects、trace_id 等）

### 步骤 5：集成 review-service 事件发布

- 在批量审核完成时发布 `review.batch.completed` 事件
- 确保事件 payload 包含必要字段（batch_id、review_results、trace_id 等）

### 步骤 6：实现事件消费重试机制

- 在 `workers/events/event_subscriber.py` 中添加重试逻辑
- 实现指数退避策略（2^n * base_delay）
- 添加最大重试次数限制
- 实现死信队列处理（超过重试次数后发送到死信队列）

### 步骤 7：补充测试用例

- 为各服务的事件发布集成添加测试
- 验证事件能正确发布到 Redis
- 验证事件消费重试机制
- 确保所有测试通过（ruff、mypy、pytest）

### 步骤 8：更新项目状态文档

- 更新 `docs/00-governance/project-status.md`
- 更新 `docs/40-dev-loop/auto-progress-log.md`

## 涉及文件与目录

### 修改文件
- `services/vote/app/api/routes.py`（集成事件发布）
- `services/content/app/api/routes.py`（集成事件发布）
- `services/generation/app/api/routes.py`（集成事件发布）
- `services/review/app/api/routes.py`（集成事件发布）
- `workers/events/event_subscriber.py`（添加重试机制）
- `docs/00-governance/project-status.md`（更新状态）

### 新增测试文件
- `services/vote/tests/test_event_publisher.py`
- `services/content/tests/test_event_publisher.py`
- `services/generation/tests/test_event_publisher.py`
- `services/review/tests/test_event_publisher.py`

## 验收标准

- [x] vote-service 投票周期关闭时发布 `vote.cycle.closed` 事件
- [x] vote-service 投票结果结算时发布 `vote.result.finalized` 事件
- [x] content-service 内容包发布时发布 `content.package.released` 事件
- [x] content-service 内容包回滚时发布 `content.package.rolled_back` 事件
- [x] generation-service 生成请求创建时发布 `generation.request.created` 事件
- [x] generation-service 批量生成完成时发布 `generation.batch.completed` 事件
- [x] review-service 批量审核完成时发布 `review.batch.completed` 事件
- [x] 事件消费重试机制实现（指数退避 + 死信队列）
- [ ] 新增测试用例全部通过
- [x] 已有测试全部通过（无回归）
- [ ] ruff check 通过
- [ ] mypy 类型检查通过
- [ ] project-status.md 已同步更新

## 风险与注意事项

- Redis 连接配置需与现有 infra 配置对齐
- 事件发布失败不应影响主业务流程（降级处理）
- 事件消费失败需有重试机制和死信队列
- 事件处理需幂等，避免重复触发
- 测试时需 mock Redis 连接