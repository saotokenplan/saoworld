# 任务计划：完善 Godot 客户端场景实现 - WorldMap、NPCPanel、QuestPanel

## 任务标识
- **task_id**: auto-20260706-0400
- **工作分支**: auto/auto-20260706-0400
- **任务状态**: 已完成

## 任务目标与背景

**背景**：项目当前处于内容发布与验证阶段，核心目标是验证端到端玩法流程。Godot 客户端工程骨架已初始化，包含完整的脚本逻辑和数据配置，但部分场景文件与脚本不匹配，导致游戏无法正常运行。

**问题识别**：
1. `WorldMap.tscn` 场景文件只包含 Background 节点，但 `world_map.gd` 脚本引用了多个不存在的节点（RegionContainer、RegionDetail、BackButton 等）
2. `NPCPanel.tscn` 场景文件缺失，但 `npc_panel.gd` 脚本已存在
3. `QuestPanel.tscn` 场景文件缺失，但 `quest_panel.gd` 脚本已存在

**目标**：完善三个核心场景文件，确保客户端场景与脚本匹配，支持世界地图浏览、NPC 交互和任务管理功能，为端到端玩法验证打好基础。

## 具体执行步骤

1. 修复 WorldMap.tscn：添加 RegionContainer、RegionDetail（含 Title、Description、Status、LevelRange）、BackButton 等节点
2. 创建 NPCPanel.tscn：包含 NPC 列表、对话触发、任务接取等 UI 元素
3. 创建 QuestPanel.tscn：包含任务列表、任务详情、目标进度、奖励展示等 UI 元素
4. 更新 Main.gd：添加 NPCPanel 和 QuestPanel 场景预加载和切换逻辑
5. 更新项目状态文档（docs/00-governance/project-status.md），记录客户端场景完善状态
6. 运行客户端测试验证场景加载正常

## 涉及的文件与目录

- `game/scenes/world/WorldMap.tscn` - 世界地图场景（需修复）
- `game/scenes/ui/npc/NPCPanel.tscn` - NPC 面板场景（需创建）
- `game/scenes/ui/quests/QuestPanel.tscn` - 任务面板场景（需创建）
- `game/scripts/Main.gd` - 主入口脚本（需更新）
- `game/scripts/world/world_map.gd` - 世界地图脚本（已存在）
- `game/scripts/ui/npc_panel.gd` - NPC 面板脚本（已存在）
- `game/scripts/ui/quest_panel.gd` - 任务面板脚本（已存在）
- `docs/00-governance/project-status.md` - 项目状态文档（需更新）

## 验收标准与 Checklist

- [x] WorldMap.tscn 包含所有脚本引用的节点（RegionContainer、RegionDetail、BackButton）
- [x] NPCPanel.tscn 创建完成，包含 NPC 列表和对话触发 UI
- [x] QuestPanel.tscn 创建完成，包含任务列表和详情展示 UI
- [x] Main.gd 支持 NPCPanel 和 QuestPanel 的场景切换
- [x] 主菜单新增 NPC 列表和任务列表按钮及信号处理
- [x] 更新项目状态文档，记录完成状态