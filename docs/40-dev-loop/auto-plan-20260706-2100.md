# 自动任务计划：修复后端服务缺失的 redis 依赖

## 任务标识
- **task_id**: auto-20260706-2100
- **工作分支**: auto/auto-20260706-2100
- **任务状态**: 已完成
- **创建时间**: 2026-07-06 21:00

## 任务目标与背景

### 目标
为 vote、content、generation、review 四个后端服务补充 redis 依赖声明，确保服务能够正常导入 event_publisher 模块，测试能够正常运行。

### 背景
在运行 vote-service 测试时发现 `ModuleNotFoundError: No module named 'redis'` 错误。经排查，有 4 个后端服务（vote、content、generation、review）的 `event_publisher.py` 模块使用了 `redis.asyncio` 进行事件发布，但它们的 `pyproject.toml` 中都没有声明 `redis` 依赖。

这是一个 P1 级别的基础设施修复任务，直接影响服务的可运行性和测试通过率。

## 具体执行步骤

### 步骤 1：为 4 个服务添加 redis 依赖
- 修改 `services/vote/pyproject.toml`，在 dependencies 中添加 `redis>=5.0.0`
- 修改 `services/content/pyproject.toml`，在 dependencies 中添加 `redis>=5.0.0`
- 修改 `services/generation/pyproject.toml`，在 dependencies 中添加 `redis>=5.0.0`
- 修改 `services/review/pyproject.toml`，在 dependencies 中添加 `redis>=5.0.0`

### 步骤 2：安装依赖并验证测试
- 为 4 个服务分别安装依赖（包含 redis）
- 运行 vote-service 测试，确认导入错误已修复
- 运行 content-service 测试
- 运行 generation-service 测试
- 运行 review-service 测试

### 步骤 3：验证其他服务
- 检查 world-service 是否真的需要 redis 依赖（当前 mypy 配置中有 redis 忽略，但代码中似乎未使用）
- 检查 player、ops、gateway 三个服务是否有类似的缺失依赖问题

### 步骤 4：更新计划文档状态
- 将任务状态更新为"已完成"

### 步骤 5：生成执行摘要
- 创建 `auto-execution-summary-20260706-2100.md`

### 步骤 6：更新进度日志
- 在 `auto-progress-log.md` 中追加本轮执行记录

## 涉及的文件与目录

### 配置文件
- `services/vote/pyproject.toml`（更新）
- `services/content/pyproject.toml`（更新）
- `services/generation/pyproject.toml`（更新）
- `services/review/pyproject.toml`（更新）

### 文档
- `docs/40-dev-loop/auto-plan-20260706-2100.md`（新增）
- `docs/40-dev-loop/auto-execution-summary-20260706-2100.md`（新增）
- `docs/40-dev-loop/auto-progress-log.md`（更新）

## 验收标准与 Checklist

### 验收标准
1. 4 个服务的 pyproject.toml 中都包含 redis 依赖声明
2. 4 个服务的测试都能正常运行（导入无错误）
3. 其他服务没有类似的缺失依赖问题

### Checklist
- [x] 为 vote-service 添加 redis 依赖
- [x] 为 content-service 添加 redis 依赖
- [x] 为 generation-service 添加 redis 依赖
- [x] 为 review-service 添加 redis 依赖
- [x] 验证 vote-service 测试正常运行（54 个测试通过）
- [x] 验证 content-service 测试正常运行（62 个测试通过）
- [x] 验证 generation-service 测试正常运行（56 个测试通过）
- [x] 验证 review-service 测试正常运行（41 个测试通过）
- [x] 检查其他服务是否有类似问题（world/player/ops/gateway 不使用 redis，无此问题）
- [x] 更新计划文档状态为已完成
- [x] 生成执行摘要
- [x] 更新进度日志
