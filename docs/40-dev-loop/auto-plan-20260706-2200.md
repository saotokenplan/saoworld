# 自动任务计划：实现 World Agent

## 任务标识
- **task_id**: auto-20260706-2200
- **工作分支**: auto/auto-20260706-2200
- **任务状态**: 已完成
- **创建时间**: 2026-07-06 22:00

## 任务目标与背景

### 目标
项目已进入 P2 多代理协同期，Product Agent、System Designer Agent、Gameplay Agent 已实现完成。本轮任务旨在实现第四个代理角色：World Agent。World Agent 负责生成 NPC、区域、事件、任务和世界描述配置，将投票结果和世界规则转化为结构化的游戏内容。

### 背景
- 当前阶段：灰度发布就绪，所有核心功能已完成
- P2 阶段目标：实现多代理协同研发闭环
- 已实现代理：Product Agent（auto-20260707-0700）、System Designer Agent（auto-20260707-0800）、Gameplay Agent（auto-20260708-0900）
- 待实现代理：World Agent、Backend Agent、QA Agent、Build Agent、Ops Agent、Orchestrator
- 技术规范：`docs/40-dev-loop/p2-agent-design/world-agent-spec.md`

## 具体执行步骤

### 步骤 1：创建代理目录结构
- 创建 `tools/agents/world_agent/` 目录
- 创建 `tools/agents/world_agent/__init__.py`

### 步骤 2：定义输入数据结构
- 创建 `input_schemas.py`：WorldRules、VoteResult、ContentTemplate、SkeletonSnapshot、DesignNote 等输入模型

### 步骤 3：定义输出数据结构
- 创建 `output_schemas.py`：NPCConfig、QuestConfig、RegionConfig、EventConfig、ContentPackageOutput、ReviewRequest 等输出模型

### 步骤 4：实现核心逻辑
- 创建 `world_agent.py`：核心代理类
  - `read_input_data()`：读取输入数据
  - `validate_input()`：校验输入
  - `match_template()`：匹配模板
  - `generate_content()`：生成内容
  - `apply_rules()`：应用规则约束
  - `refine_text()`：生成文本润色
  - `package_content()`：打包内容
  - `submit_for_review()`：提交审核
  - `handle_review_result()`：处理审核结果
  - `execute_world_generation_flow()`：执行完整世界生成流程

### 步骤 5：实现错误处理
- 创建 `error_handler.py`：骨架快照缺失、模板不匹配、内容违规、审核失败、重复度过高的处理逻辑

### 步骤 6：实现 CLI 接口
- 创建 `cli.py`：命令行工具，支持 `generate-npc`、`generate-quest`、`generate-region`、`run-workflow` 命令

### 步骤 7：编写测试用例
- 创建 `tests/test_world_agent.py`：单元测试覆盖核心流程

### 步骤 8：验证测试通过
- 运行 pytest 验证所有测试通过

## 涉及的文件与目录

### 新增目录
- `tools/agents/world_agent/`（World Agent 目录）

### 新增文件
- `tools/agents/world_agent/__init__.py`
- `tools/agents/world_agent/input_schemas.py`
- `tools/agents/world_agent/output_schemas.py`
- `tools/agents/world_agent/world_agent.py`
- `tools/agents/world_agent/error_handler.py`
- `tools/agents/world_agent/cli.py`
- `tools/agents/world_agent/tests/test_world_agent.py`

### 更新文件
- `docs/40-dev-loop/auto-progress-log.md`（进度日志追加）
- `docs/00-governance/project-status.md`（记录 World Agent 实现完成）

## 验收标准与 Checklist

### 验收标准
1. World Agent 代码结构完整
2. 所有输入输出数据结构定义正确
3. 核心流程（9步）实现完整
4. 错误处理机制完善（骨架快照缺失、模板不匹配、内容违规、审核失败、重复度过高）
5. CLI 命令行工具可用（generate-npc、generate-quest、generate-region、run-workflow）
6. 测试用例覆盖核心功能
7. 所有测试通过

### Checklist
- [x] 创建代理目录结构
- [x] 定义输入数据结构
- [x] 定义输出数据结构
- [x] 实现核心逻辑
- [x] 实现错误处理
- [x] 实现 CLI 接口
- [x] 编写测试用例
- [x] 验证测试通过（25 个测试全部通过）
- [x] 更新项目状态文档
- [x] 提交代码并合并
