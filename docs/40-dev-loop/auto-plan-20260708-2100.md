# 自动任务计划：审计日志异常处理修复与错误码完善

## 任务标识
- **task_id**: auto-20260708-2100
- **工作分支**: auto/auto-20260708-2100
- **任务状态**: 已完成
- **创建时间**: 2026-07-08 21:00

## 任务目标与背景

### 目标
修复多个后端服务中审计日志写入异常被静默吞没的问题（`except Exception: pass`），将其替换为 structlog 错误日志记录，提升系统可观测性和安全合规性。同时检查并补全 `CANDIDATE_NOT_ACTIVE` 错误码的使用。

### 背景
- 通过代码审查发现：vote-service、content-service、generation-service、review-service 的 routes.py 中存在多处 `except Exception: pass`，用于审计日志写入失败时的异常处理
- 这些静默吞没导致审计日志写入失败完全不可见，违反了 `10-python-backend.md` 中"审计日志持久化"的安全要求
- 项目已定义 `AUDIT_WRITE_FAILED` 错误码但从未使用
- `CANDIDATE_NOT_ACTIVE` 错误码在 `12-api-design.md` 中定义，但需检查是否在代码中正确使用
- 所有 8 个后端服务已集成 structlog，具备替换条件

## 具体执行步骤

### 步骤 1：检查所有服务的审计日志异常处理
- 逐个检查 8 个服务的 routes.py，定位所有 `except Exception: pass` 模式
- 区分合法的 pass（如 ValueError 转换失败）和不合法的 pass（审计日志写入失败）

### 步骤 2：修复审计日志异常处理
- 将 `except Exception: pass` 替换为 `except Exception as exc: logger.error("audit_write_failed", ...)` 
- 在每个服务的 routes.py 中添加 structlog logger（如尚未导入）
- 使用项目已定义的 `AUDIT_WRITE_FAILED` 错误码作为日志事件标识

### 步骤 3：检查 CANDIDATE_NOT_ACTIVE 错误码使用
- 检查 vote-service 的 submit_vote 端点是否正确返回 `CANDIDATE_NOT_ACTIVE`
- 如果候选项状态为 withdrawn 或 selected 时应返回此错误码

### 步骤 4：检查其他服务的静默异常
- 检查 review-service、world-service、player-service、ops-service 的 routes.py
- 修复发现的类似问题

### 步骤 5：运行测试验证
- 运行所有修改的服务的测试
- 确保修改不破坏现有功能

### 步骤 6：更新项目状态文档
- 更新 project-status.md

## 涉及的文件与目录

### 修改文件
- `services/vote/app/api/routes.py`（审计日志异常处理修复 + CANDIDATE_NOT_ACTIVE 检查）
- `services/content/app/api/routes.py`（审计日志异常处理修复）
- `services/generation/app/api/routes.py`（审计日志异常处理修复）
- `services/review/app/api/routes.py`（审计日志异常处理修复）
- `services/world/app/api/routes.py`（如存在类似问题）
- `services/player/app/api/routes.py`（如存在类似问题）
- `services/ops/app/api/routes.py`（如存在类似问题）
- `docs/00-governance/project-status.md`（状态更新）

### 新增文件
- `docs/40-dev-loop/auto-execution-summary-20260708-2100.md`

## 验收标准与 Checklist

### 验收标准
1. 所有服务中审计日志写入的 `except Exception: pass` 已替换为带 structlog 日志的错误处理
2. `CANDIDATE_NOT_ACTIVE` 错误码在 vote-service 中正确使用（如适用）
3. 所有 8 个后端服务测试通过
4. ruff 和 mypy 检查通过

### Checklist
- [x] 步骤 1：检查所有服务的审计日志异常处理
- [x] 步骤 2：修复审计日志异常处理
- [x] 步骤 3：检查 CANDIDATE_NOT_ACTIVE 错误码
- [x] 步骤 4：检查其他服务的静默异常
- [x] 步骤 5：运行测试验证
- [x] 步骤 6：更新项目状态文档
