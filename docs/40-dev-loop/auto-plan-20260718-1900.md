# 自动推进任务计划：M3-04/M3-05 Alembic 迁移脚本补全

> 任务标识：auto-20260718-1900
> 任务状态：已完成
> 工作分支：auto/auto-20260718-1900
> 创建时间：2026-07-18 19:00
> 完成时间：2026-07-18 19:30
> 优先级：P1（灰度发布前基础设施缺口）

## 任务背景

项目当前处于 M3 里程碑完成、等待灰度发布决策阶段。M3-04「跨服匹配系统」与 M3-05「赛季排行系统」已完成后端代码与测试，但对应的 Alembic 迁移脚本未补全，存在以下缺口：

1. **M3-04 跨服匹配系统**：新增 5 张核心表（`match_seasons`、`player_ratings`、`match_queues`、`match_rooms`、`match_results`），未生成 Alembic 迁移脚本
2. **M3-05 赛季排行系统**：新增 1 张表（`season_reward_grants`），并扩展 `match_seasons` 表新增 `settlement_status` 和 `settled_at` 字段，未生成 Alembic 迁移脚本

依据 `42-release-rollback.md` 规范的"发布前检查清单"——"数据库迁移脚本已评审并在预发布环境验证"是发布前强制门禁，本任务补全这两次模型变更对应的迁移脚本，闭合灰度发布前的基础设施缺口。

## 任务目标

为 M3-04/M3-05 数据库变更补全 Alembic 迁移脚本：

1. 创建 M3-04 迁移脚本：5 张匹配系统表的完整 DDL（含 CHECK 约束、索引、唯一约束）
2. 创建 M3-05 迁移脚本：`season_reward_grants` 表 + `match_seasons` 表字段扩展
3. 迁移脚本与 `services/player/app/domain/models.py` 中的模型定义严格一致
4. 提供 `upgrade()` 与 `downgrade()` 双向操作
5. 运行 player-service 测试套件确认无回归

## 涉及的表与字段

### M3-04 新增表（5 张）

| 表名 | 用途 | 关键约束 |
|------|------|---------|
| `match_seasons` | 匹配赛季 | status 状态机（upcoming/active/ended/archived），settlement_status 状态机（unsettled/settling/settled） |
| `player_ratings` | 玩家段位 | UNIQUE (player_id, season_id)，tier/division CHECK 约束 |
| `match_queues` | 匹配队列 | status/match_mode/tier CHECK 约束 |
| `match_rooms` | 对战房间 | status/match_mode CHECK 约束 |
| `match_results` | 对战结果 | room_id UNIQUE，match_mode CHECK 约束 |

### M3-05 新增表（1 张）

| 表名 | 用途 | 关键约束 |
|------|------|---------|
| `season_reward_grants` | 赛季奖励发放 | UNIQUE (season_id, player_id)，UNIQUE (idempotency_key)，final_tier/final_division/final_rank/status CHECK 约束 |

### M3-05 字段扩展

在 `match_seasons` 表上新增：
- `settlement_status VARCHAR(32) NOT NULL DEFAULT 'unsettled'`，CHECK 约束限定 unsettled/settling/settled
- `settled_at TIMESTAMPTZ NULL`
- 索引 `match_seasons_settlement_idx`

## 执行步骤与验收清单

- [x] 步骤 1：创建 M3-04 迁移脚本 `2026_07_18_1900_add_match_system_tables.py`，包含 5 张表完整 DDL
- [x] 步骤 2：创建 M3-05 迁移脚本 `2026_07_18_1901_add_season_reward_grants.py`，包含 1 张新表 + match_seasons 字段扩展
- [x] 步骤 3：校验迁移脚本与 `models.py` 字段、类型、约束、索引严格一致
- [x] 步骤 4：运行 `pytest services/player/tests/` 确认 309 个测试全部通过
- [x] 步骤 5：运行 `ruff check services/player/alembic/` 确认代码质量
- [x] 步骤 5.5：修复历史迁移链路断裂（2026_07_14_1300 与 2026_07_16_1800 的 down_revision 错误）
- [x] 步骤 6：更新 `docs/00-governance/project-status.md` 标记本任务完成
- [x] 步骤 7：追加进度日志 `docs/40-dev-loop/auto-progress-log.md`
- [x] 步骤 8：生成执行摘要文档
- [ ] 步骤 9：按主题拆分提交并合并到 feature-prd（进行中）

## 验收标准

1. 两个迁移脚本文件存在于 `services/player/alembic/versions/` 目录
2. 迁移脚本 `down_revision` 链正确：`2026_07_17_0801` → M3-04 → M3-05
3. 迁移脚本字段、类型、CHECK 约束、索引、唯一约束与 `models.py` 严格一致
4. player-service 309 个测试全部通过，无回归
5. ruff 检查 0 错误
6. 工作分支合并到 feature-prd

## 风险与回滚

- **风险**：迁移脚本与模型定义不一致可能导致生产环境迁移失败
- **缓解**：逐字段对照 `models.py` 编写，运行测试套件验证
- **回滚**：每个迁移脚本提供完整 `downgrade()` 实现，可双向回滚
