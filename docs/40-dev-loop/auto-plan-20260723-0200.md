# 自动推进计划：M4 阈值调优预案（WP2 规划期准备）

> 任务标识：auto-20260723-0200
> 任务状态：已完成
> 工作分支：auto/auto-20260723-0200
> 生成时间：2026-07-23 02:04

## 任务目标与背景

### 选定工作

`docs/00-governance/project-status.md`「下一阶段建议」第 5 条：M4 规划期仅推进模板细化、规则调优预案等低风险准备工作。`docs/10-requirements/M4-规模化内容生成规划.md` 第五节白名单五项：模板文本细化、审核样本集设计、阈值调优预案、看板指标定义、运营流程草案。

本轮选定：**阈值调优预案**（WP2「自动审核规则调优」的第二项规划期准备）。

### 选定理由

- 审核样本集设计（auto-20260723-0040）已完成并合并（merge 7594b27 / 11e0468），阈值调优预案是其直接下游：预案的回归门禁、样本版本与测量口径全部引用样本集设计，口径已冻结，依赖就绪。
- WP2 对应差距 G4（自动审核通过率 >70%）为 M4 最大缺口；预案把「怎么调、按什么顺序调、调完怎么验收」前置固化，避免实施期在真实样本到位后临场决策。
- 纯设计文档产出：不改核心链路代码、不接入新外部依赖、不需要 PostgreSQL / Redis / Docker 运行时，完全符合「不抢跑」约束。
- 其余白名单项（看板指标定义 WP3、运营流程草案 WP4、模板文本细化 WP1）无样本集依赖，留作后续轮次候选。

### 前序中断任务处置

00:40 轮次（auto-20260723-0040）在内容完成后中断于提交前。本轮（01:55 起）已先行接续完成其 checklist 回填、进度日志追加、两笔主题提交、两次 `--no-ff` 合并与远程推送校验，并删除其本地工作分支；本任务在其干净基线（feature-prd @ 11e0468）上启动。

### 动态选专家记录

- 任务类型：文档 / 规范设计（内容审核与质量工程域）。按映射应选「技术写作或文档规范类专家」或质量工程类专家。
- 实际情况：自动化执行环境无专家会话调用通道，本轮跳过实际专家调用，直接按 `.trae/rules/`（02、40、51、52）与既有代码（`services/review/app/core/auto_review_engine.py`）执行。
- 校准要点（来自规则与代码自审）：预案参数清单必须以代码真实常量为准；发现 `DuplicateDetectionRule.max_similarity` 为未接线死参数，须作为实施期改造项记录而非假装可调；安全词表与安全一票否决列入不可调边界。

## 具体执行步骤

1. [x] 读取 project-status.md、M4 规划文档、M4 审核样本集设计、auto_review_engine.py，确认任务边界与代码口径
2. [x] 从 feature-prd 切出工作分支 auto/auto-20260723-0200
3. [x] 编写 `docs/10-requirements/M4-阈值调优预案.md`（初始状态 draft）
4. [x] 同步引用：`docs/10-requirements/M4-规模化内容生成规划.md` 第五节进展 + 第八节回链；`docs/10-requirements/README.md` 补登本文档
5. [x] 循环日志 `.trae/loop-log/round-5.md`
6. [x] 执行摘要 `docs/40-dev-loop/auto-execution-summary-20260723-0200.md` + 追加 `auto-progress-log.md`
7. [x] 按主题拆分提交并逐笔推送 origin，--no-ff 合并回 feature-prd 并推送，校验后清理分支

## 涉及文件与目录

| 文件 | 操作 |
|------|------|
| docs/10-requirements/M4-阈值调优预案.md | 新增（主交付物） |
| docs/10-requirements/M4-规模化内容生成规划.md | 修改（第五节进展 + 第八节回链） |
| docs/10-requirements/README.md | 修改（文档列表补登） |
| docs/40-dev-loop/auto-plan-20260723-0200.md | 新增（本文档） |
| docs/40-dev-loop/auto-execution-summary-20260723-0200.md | 新增 |
| docs/40-dev-loop/auto-progress-log.md | 修改（追加快照） |
| .trae/loop-log/round-5.md | 新增 |

不触碰：services/、game/、workers/、infra/、.trae/rules/、project-status.md（项目级状态无本质变化，遵守其维护原则）。

## 验收标准与 Checklist

- [x] 设计文档含 52-documentation 要求的头部元信息与必备章节（适用范围 / 当前定位 / 核心内容 / 与其他文档的关系）
- [x] 参数清单覆盖引擎全部规则常量（0.8 / 0.5 / 0.85 / 0.3 / 20 字符 / 17 词表 / 规则顺序），逐项标注可调性与所在位置
- [x] 明确记录 `DuplicateDetectionRule.max_similarity=0.8` 为未接线死参数，列为实施期改造前置项
- [x] 给出分场景调优预案（通过率不足 / 人工率过高 / 完整度误拒集中 / 重复度漏检），每场景含候选值、预期影响、门禁检查与回退口径
- [x] 回归门禁与样本集设计一致（安全误放率 = 0 不可调、应过误拒率 < 15%、人工自动终结比例不上升、样本级归因）
- [x] 明确规划期边界：不改代码与常量、不以合成样本宣称通过率达标、安全词表变更走安全评审
- [x] 所有引用使用相对路径，无失效引用
- [x] 测试无需变更（纯文档任务，未改 services/ 代码）
- [x] 提交遵循 40-git-workflow 规范并全部落到 origin；合并提交落到 origin/feature-prd
