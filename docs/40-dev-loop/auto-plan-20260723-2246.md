# 自动工作计划：auto-20260723-2246

> 任务标识：auto-20260723-2246
> 生成时间：2026-07-23 22:46
> 工作分支：auto/auto-20260723-2246
> 任务状态：已完成
> 关联工作包：M4-WP1 生成模板验收收口 → 实施期动作 A2（F1 缺陷修复）
> 优先级：P0（M4 验收 G3 场景评分维度修复，纯代码、可单测、无运行时依赖）

## 任务目标与背景

M4 实施期已于 2026-07-23 21:46 经管理决策解锁（`M4 决策落地——跳过灰度阶段`）。WP1 生成模板验收收口的实施期动作清单（见 `docs/10-requirements/M4-模板文本细化.md` 第五节）中，**A2（`score_region` 字段口径对齐）依赖标注为「无（可与 A1 并行）」，是首个可自主、可单测、不依赖运行时环境推进的动作**，故作为 M4 实施期首个代码落地项。

**缺陷 F1（高严重度）**：`services/generation/app/core/quality_scorer.py::score_region` 校验 `difficulty`（easy/normal/hard/extreme）与 `features`，但 live prompt 与 `region_data_adapter` 实际产出字段为 `danger_level`（peaceful/low/medium/high/extreme）与 `landmarks`。经代码实测确认（`content_generator.py:214` 先经 `region_adapter.ensure_minimum_completeness` 归一化），运行时进入评分器的对象**永远不含 `difficulty`/`features`**，因此：
- `difficulty` 枚举校验永不命中（恒为缺省，等于无校验）；
- `features` 恒为空列表，每次场景评分被静默扣 `-0.2`；
- 场景生成「评分维度」名存实亡，直接威胁 M4 验收 G3（场景描述生成质量稳定）。

## 具体执行步骤（每步可独立验收）

1. **代码修复**：重写 `score_region`，将校验字段由 `difficulty`/`features` 对齐为 `danger_level`（含枚举集 peaceful/low/medium/high/extreme）与 `landmarks`（非空列表 + 元素结构校验：landmark_key/name/description/type/significance）。枚举值对齐 `region_data_adapter.VALID_DANGER_LEVELS`。
2. **测试同步**：更新 `services/generation/tests/test_quality_scorer.py` 中 `test_score_region_valid`、`test_score_region_invalid_difficulty`、以及 `test_score_method_dispatch` 的 region 用例，改用 `danger_level`/`landmarks` 字段，新增 landmark 元素结构缺失的扣分断言。
3. **运行测试**：在 `services/generation` 下运行 pytest，确认全量用例通过（重点 `test_quality_scorer` 与 `test_content_generator` 不被回归）。
4. **规范回填**：将 `docs/10-requirements/M4-模板文本细化.md` 第三节 §3.6 表格 A2 行回填为「已落地」，并标注落地提交。
5. **状态记录**：更新 `project-status.md` 当前状态为「WP1 实施已落地首个代码项（A2）」，追加执行摘要 `auto-execution-summary-20260723-2246.md` 与进度日志。
6. **提交与合并**：按主题拆分提交（`fix(generation)` 代码、`test(generation)` 测试、`docs(requirements)` 规范回填），逐笔推送 `origin/auto/auto-20260723-2246`，`--no-ff` 合并回 `feature-prd` 并推送到 `origin/feature-prd`。

## 涉及的文件与目录

- `services/generation/app/core/quality_scorer.py`（修改 `score_region`，L225-251）
- `services/generation/tests/test_quality_scorer.py`（修改 3 个 region 测试用例）
- `docs/10-requirements/M4-模板文本细化.md`（§3.6 表格 A2 回填「已落地」）
- `docs/00-governance/project-status.md`（当前状态轻量更新）
- `docs/40-dev-loop/auto-execution-summary-20260723-2246.md`（新增执行摘要）
- `docs/40-dev-loop/auto-progress-log.md`（追加执行记录）

## 验收标准与 checklist

- [x] `score_region` 校验 `danger_level`（5 值枚举）与 `landmarks`（非空 + 元素结构），不再引用 `difficulty`/`features`
- [x] `services/generation` 全部 pytest 用例通过（无回归、无新失败）—— 45 passed
- [x] 旧 `test_score_region_invalid_difficulty` 重写为 `test_score_region_invalid_danger_level` 并补充 landmark 结构校验断言
- [x] `M4-模板文本细化.md` §3.6 A2 行标记为「已落地」
- [x] 提交信息遵循 `<type>(<scope>): <summary>`（scope=generation/requirements）
- [x] 工作分支逐笔推送 origin；合并提交落到 `origin/feature-prd`
- [x] 本地工作分支在合并推送成功后删除

## 所选专家与咨询要点

- **本轮未调用独立专家会话**。依据 `M4-模板文本细化.md` §3.6（A2 字段口径对齐表）作为权威设计依据；任务类型为纯后端 Python 评分逻辑修复，规范已闭合，无需外部专家阻塞审批。
- 专家非必须阻塞环节；如后续 A1（双源收口迁移）涉及 `generate_*` 调用链重构，届时再按模块选择后端/架构类专家。

## 风险与备注

- 修复后运行时若 LLM 未产出 `landmarks`，评分将被扣 `-0.2`（符合 §3.6「补非空与元素结构校验」意图），推动 prompt（F6）与实测（A6）补齐，与 WP1 验收口径一致。
- 不涉及 `difficulty`/`features` 旧字段的兼容保留：运行时路径不产生这两字段，`content_generator` 区域模板测试（`test_content_generator.py` 中 `difficulty`/`features` 断言）属于 F0 双源模板（A1 范围），不在本轮修改。
