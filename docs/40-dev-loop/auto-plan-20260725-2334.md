# 自动推进计划：WP1 A3/A4/A5 提示词硬化落地

> 任务标识：auto-20260725-2334
> 任务状态：已完成
> 工作分支：auto/auto-20260725-2334
> 生成时间：2026-07-25 23:34

## 任务目标与背景

### 选定工作

`docs/00-governance/project-status.md`「当前待办（活跃工作）」明确：WP1 模板验收收口进行中，A2（F1 修复）已落地；**A3/A4/A5「不依赖运行时可立即推进」**。本轮选定 WP1 实施期动作 **A3（数值锚定表 F2/F3）+ A4（章节强度上限条款 F4）+ A5（输出格式硬化 F6）**，将 `docs/10-requirements/M4-模板文本细化.md` 第三节的硬化文本落地至 generation 服务 live prompt 构造函数。

### 选定理由

- 灰度阶段已跳过、M4 进入实施期，「不抢跑」约束解除；project-status.md 明确授权 A3/A4/A5 立即推进。
- A1（双源收口迁移至 jinja2）与 A6（真实 LLM 实测）依赖真实运行时环境，本轮不可为；A3/A4/A5 仅为 live prompt 文本插入，不依赖运行时，可独立落地并以单测校验。
- 直接落 live `_build_*_prompt` 函数尾部，与 A1 后续迁移不冲突（A1 实施时一并搬运文本至 jinja2）。

### 动态选专家记录

- 任务类型：后端 / Python（LLM 提示词文本构造，提示词工程交叉域）。按映射应选「后端 / API 设计或架构类专家」或「技术写作类专家」。
- 实际情况：自动化执行环境无专家会话调用通道（与历轮先例一致），跳过实际专家调用，直接按 `.trae/rules/`（02 agent-loop、40 git-workflow、52 documentation）与代码实测执行。
- 校准要点：硬化文本须锚定**实际生效的 live 内联 prompt**（F0 结论，jinja2 为死模板）；文本块直接取自 M4-模板文本细化.md 3.1/3.2/3.3/3.4，避免口径漂移；few-shot 开关（3.4 第二段）依赖 WP2 样本 JSON 内容，本轮不接入。

## 具体执行步骤

1. [x] 读取 project-status.md、M4-模板文本细化.md、content_generator.py，确认 A3/A4/A5 为唯一可立即推进的活跃工作
2. [x] 从 feature-prd 切出工作分支 auto/auto-20260725-2334
3. [x] A3（F2/F3）：3.1 装备数值锚定表写入 `_build_item_prompt`；3.2 怪物数值锚定表写入 `_build_monster_prompt`；Boss prompt 显式引用 3.2 公式
4. [x] A4（F4）：3.3 章节一致性约束写入 item/monster/boss/region 四类 prompt 尾部
5. [x] A5（F6）：3.4 输出格式硬化文本全量接入 item/monster/boss/region 四类 prompt（few-shot 开关因依赖 WP2 样本内容推迟，记录为 defer）
6. [x] 测试：`test_content_generator.py` 将 `generator` fixture 提升为模块级，新增 `TestPromptHardening` 断言四类 prompt 含硬化文本；运行 pytest + ruff 全绿
7. [x] 文档回填：M4-模板文本细化.md 第五节动作表状态 + 实施期落地记录；project-status.md 当前状态/当前待办更新
8. [x] 执行摘要 `auto-execution-summary-20260725-2334.md` + 追加 `auto-progress-log.md`
9. [x] 按主题拆分提交并逐笔推送 origin 工作分支；`--no-ff` 合并回 feature-prd 并推送；校验后清理分支

## 涉及文件与目录

| 文件 | 操作 |
|------|------|
| services/generation/app/core/content_generator.py | 修改（A3/A4/A5 硬化文本插入四类 prompt 构造函数） |
| services/generation/tests/test_content_generator.py | 修改（fixture 提级 + 新增 TestPromptHardening 4 例） |
| docs/10-requirements/M4-模板文本细化.md | 修改（动作表状态 + 实施期落地记录） |
| docs/00-governance/project-status.md | 修改（当前状态/当前待办 WP1 进度） |
| docs/40-dev-loop/auto-plan-20260725-2334.md | 新增（本文档） |
| docs/40-dev-loop/auto-execution-summary-20260725-2334.md | 新增 |
| docs/40-dev-loop/auto-progress-log.md | 修改（追加快照） |

不触碰：A1 双源收口（依赖运行时）、A6 实测（依赖运行时）、services/ 其他模块、game/、workers/、infra/、.trae/rules/、.workbuddy/ 遥测（留待周期性收拢，不进主题提交）。

## 验收标准与 Checklist

- [x] A3 落地：item prompt 含装备数值锚定（weapon/armor 基值公式、稀有度系数表）；monster prompt 含怪物数值锚定（hp/attack/defense 基值 + 类型系数表）；boss prompt 显式引用 3.2 公式（hp≥5×、attack≥2×）
- [x] A4 落地：item/monster/boss/region prompt 尾部均含「章节一致性约束（F4）」条款，章节上限条款与 3.3 一致
- [x] A5 落地：item/monster/boss/region prompt 尾部均含「输出格式要求（F6）」（仅返回 JSON、禁 markdown 围栏、自检必需字段）
- [x] few-shot 开关明确记录为 defer（依赖 WP2 样本 JSON 内容），未制造空开关半成品
- [x] `pytest services/generation/tests/test_content_generator.py` 全绿（14 passed，含 4 新增硬化落盘校验），ruff 通过
- [x] 文档与代码一致：M4-模板文本细化.md 动作表状态、project-status.md 进度同步更新
- [x] 合并提交落到 origin/feature-prd，本地工作分支已删除
