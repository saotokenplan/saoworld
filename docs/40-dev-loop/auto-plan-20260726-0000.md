# 自动工作计划：auto-20260726-0000

> 任务标识：auto-20260726-0000
> 工作分支：auto/auto-20260726-0000
> 创建时间：2026-07-26 00:00
> 关联工作包：M4-WP2 自动审核规则调优（实施期改造前置项）
> 任务状态：已完成

## 一、任务目标与背景

`docs/10-requirements/M4-阈值调优预案.md` 第二节「已发现死参数」显式登记：

> `DuplicateDetectionRule.max_similarity = 0.8`：构造函数接收并存储，但 `evaluate()` **从未使用**。当前重复度规则只做单对象 description 内部字符多样度检测……该需求须先完成实施期改造（实现跨样本相似度计算并接线 `max_similarity`），之后该参数才进入调优空间。

这是 WP2 实施期改造前置项（死参数），也是规划期已识别、待实施期落地的明确代码缺口。当前 `evaluate()` 硬编码 `diversity_ratio < 0.3` 阈值，完全忽略 `max_similarity`，导致 WP2「相似度 <0.8 实测验证」无调优对象、且参数默认值 0.8 在代码中不生效。

**本轮目标**：将 `max_similarity` 真正接线为重复度判定阈值，使默认值 0.8 生效；同时补齐跨样本字符级 Jaccard 相似度纯函数能力（WP2 场景 D 改造项的函数基础），为后续运行时语料注入接线做准备。该任务**不依赖真实运行时环境**，可由 pytest 完全验证，是 WP2 首个可自主落地的代码项。

## 二、专家选择（第四步之二）

- 本轮为 `services/review` 纯代码改造（Python / 审核规则引擎），属后端代码范围。
- 专家中心无对应「可调用专家会话」工具入口，按规则跳过专家调用，直接依据 `.trae/rules/`（40-git-workflow、41-testing、02-agent-loop-constraints）与既有代码执行。
- 不沿用上一轮（auto-20260725-2334，generation 文本硬化）的专家判断，本轮独立判定为「无需专家」。

## 三、具体执行步骤（每步可独立验收）

1. **接线 `max_similarity` 到 `DuplicateDetectionRule.evaluate`**
   - 在 `services/review/app/core/auto_review_engine.py` 中将自相似度口径定义为 `1 - 字符多样度`，当 `self_similarity >= max_similarity` 时判 `rejected`（默认 0.8 → 自相似度 ≥0.8 拦截）。
   - 验收：构造函数默认值 0.8 在判定中实际生效；移除原硬编码 0.3 常量。
2. **补充纯函数能力**
   - 新增静态方法 `self_similarity(description)` 与 `cross_similarity(a, b)`（字符级 Jaccard），为 WP2 场景 D 跨样本相似度接线提供可单测基础。
   - 验收：两方法为纯函数、无副作用、空串安全。
3. **同步测试**
   - 新增 `services/review/tests/test_duplicate_detection_rule.py`，覆盖：自相似度受控数值、跨样本 Jaccard 数值、短描述跳过、高重复拦截、多样长文本通过、参数真正被使用（放宽阈值即不拦截）、默认值 0.8。
   - 验收：pytest 全部通过，新增文件为本次提交唯一测试变更。
4. **运行验证**
   - 在 review venv 下运行 `pytest services/review/tests/test_duplicate_detection_rule.py` 与 review 全量测试，确认无回归。
   - 验收：目标测试文件全绿；review 服务整体测试无新增失败。
5. **文档回填**
   - 更新 `M4-阈值调优预案.md` 死参数行状态为「已接线（2026-07-26, auto-20260726-0000）」；更新 `project-status.md` 当前阶段/当前待办中 WP2 标记为「进行中（首个代码项已落地）」。
   - 验收：两文档状态与代码一致。

## 四、涉及的文件与目录

- 修改：`services/review/app/core/auto_review_engine.py`
- 新增：`services/review/tests/test_duplicate_detection_rule.py`
- 文档：`docs/10-requirements/M4-阈值调优预案.md`、`docs/00-governance/project-status.md`
- 闭环：`docs/40-dev-loop/auto-plan-20260726-0000.md`、`docs/40-dev-loop/auto-execution-summary-20260726-0000.md`、`docs/40-dev-loop/auto-progress-log.md`

## 五、验收标准与 Checklist

- [x] `DuplicateDetectionRule.max_similarity` 在 `evaluate` 中实际生效（不再硬编码 0.3）
- [x] 默认值 0.8 与 `M4-阈值调优预案.md` 登记一致
- [x] `self_similarity` / `cross_similarity` 纯函数补充且空串安全
- [x] `test_duplicate_detection_rule.py` 全部通过（14 passed）
- [x] review 服务全量 pytest 无回归（79 passed；修复 greenlet 测试依赖缺口）
- [x] `M4-阈值调优预案.md` 死参数行回填「已接线」
- [x] `project-status.md` WP2 状态更新为进行中
- [x] 提交遵循 `40-git-workflow.md`（feat/test/fix/docs 分主题），工作分支推送 origin，合并回 origin/feature-prd 并 fetch 校验

## 六、范围与边界说明

- **不做**：不接入跨样本语料（需 PostgreSQL 运行时中的既有对象集合），场景 D 的完整跨样本接线留待运行时验证阶段；本轮仅补齐函数基础与单对象阈值接线。
- **不做**：不对 `max_similarity` 数值做调优（规划期/预案明确禁止在无真实样本回放前提下降/升阈值），仅将其接线为预案登记的默认 0.8。
- 单轮产出远小于 50 文件 / 2000 行上限，无需拆分。
