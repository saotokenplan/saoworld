# 自动工作计划：auto-20260726-1029

> 任务标识：`auto-20260726-1029`
> 工作分支：`auto/auto-20260726-1029`（自 `feature-prd` 切出）
> 创建时间：2026-07-26 10:29
> 任务状态：已完成（接续 10:29 中断轮次，12:10 轮完成验证/测试修复/提交/合并）

## 一、任务目标与背景

M4 工作包 **WP5 批量生成能力**（原 S10-07，差距 G8：`批量生成多个区域/怪物/装备的批处理能力未确认，可能需新建`）。

经代码核查确认：
- `services/generation/app/core/content_generator.py` 仅提供 `generate_npc/quest/region/settlement/monster/boss/item` **单对象**生成方法，无任何批量入口。
- `services/generation/app/api/routes.py` 仅有一个单区域生成端点 `/generation/regions`，无批量端点。
- `event_publisher` 已具备 `publish_generation_batch_completed` 事件，但无任何调用方——批处理链路意图已预埋、实现缺失。

本轮在**不依赖真实运行时**（PostgreSQL/Redis/Docker/真实 LLM）前提下，落实 WP5 首个可执行子任务「确认现有生成链路对批量请求的支持程度，补齐批量入口」：
1. 在 `ContentGenerator` 新增 `generate_batch` 方法（失败隔离 + 并发上限 + 成本上限）。
2. 新增批量请求/响应 schema。
3. 新增 `POST /api/v1/ops/generation/batch` 运营端点（可选持久化 + 事件发布）。
4. 同步补充测试，全部基于 MockLLMAdapter + 内存 SQLite，可在无运行时环境跑通。

## 二、动态专家选择

- 任务类型为后端 / Python / API（generation 服务）。专家中心无直接匹配的后端生成专家；参照历史轮次（auto-20260726-0856 / -0000 / -2334）做法，本轮**跳过专家调用**，依据 `.trae/rules/` 与 `content_generator.py` 既有模式直接实现。

## 三、具体执行步骤（每步可独立验收）

- [x] 步骤 1：`config.py` 新增 `batch_max_items` / `batch_max_concurrency` / `batch_token_budget_per_item` / `batch_token_budget` 配置项
- [x] 步骤 2：`schemas/generation.py` 新增 `BatchItemRequest` / `BatchGenerateRequest` / `BatchItemResponse` / `BatchGenerateResponse`
- [x] 步骤 3：`content_generator.py` 新增 `BatchItemSpec` / `BatchItemResult` / `BatchResult` 数据类与 `generate_batch` 方法（含失败隔离、并发上限、成本上限）
- [x] 步骤 4：`routes.py` 新增 `POST /generation/batch` 端点（校验、成本门禁、可选持久化、事件发布）
- [x] 步骤 5：`tests/test_batch_generation.py` 新增方法级 + 端点级测试（含失败隔离、非法类型、成本上限、持久化）
- [x] 步骤 6：运行 `services/generation` 全量 pytest 与 ruff，确保全绿（266 passed / ruff 无新增）
- [x] 步骤 7：更新 `docs/00-governance/project-status.md`（WP5 标记进行中）+ `M4-规模化内容生成规划.md` WP5 状态回填

## 四、涉及的文件与目录

- `services/generation/app/core/config.py`
- `services/generation/app/schemas/generation.py`
- `services/generation/app/core/content_generator.py`
- `services/generation/app/api/routes.py`
- `services/generation/tests/test_batch_generation.py`（新增）
- `docs/00-governance/project-status.md`
- `docs/10-requirements/M4-规模化内容生成规划.md`

## 五、验收标准与 checklist

- [x] `generate_batch` 对混合批量请求返回 `BatchResult`，成功项含 `data`，失败项被隔离且不影响其余项
- [x] 非法 `target_type` 被识别为单项失败（不中断整体）
- [x] 超过 `batch_max_items` 或预估 token 超 `batch_token_budget` 时拒绝（成本上限生效）
- [x] 端点 `POST /api/v1/ops/generation/batch` 返回统一 envelope，字段含 `total/succeeded/failed/items`
- [x] 端点 `persist=true` 时成功项写入 `generated_objects`（状态 `pending_review`）
- [x] `pytest services/generation` 全量通过；`ruff` 对改动文件无新增问题
- [x] 文档状态同步（WP5 进行中）
